# バランス検証の記録（M18: T-M18-03 / T-M18-04 / T-M18-05）

測定日: 2026-08-10
測定機: darwin / Node v24 / `vitest run`（`vitest.config.ts` の `fileParallelism: false` に依存）

このファイルは **実測値の記録**である。目標に届いていない項目も、届いていないと書く。

---

## 1. T-M18-04 文明バランス（AI 同士の総当たり勝率）

完了条件: **8 文明の勝率が 50%±8% 以内。逸脱は JSON のみで調整**

### 1.1 測定条件

| 項目 | 値 |
|---|---|
| テスト | `game/tests/balance/civ.matchup.test.ts` |
| 組み合わせ | 8 文明の総当たり **28 組** |
| 左右入れ替え | **する**（同じ組を playerId 0/1 を入れ替えて 2 回） |
| シード | 3 本（`20260810` / `777` / `31337`） |
| 試合数 | 28 × 2 × 3 = **168 試合** |
| 1 試合の上限 | **15,000 tick**（= 600 秒 = 10 分。25 tick/秒） |
| AI 段階 | **4「将軍」**（判断間隔 2 秒、令 6 種、戦域 6 本、攻城あり） |
| マップ | 既定（`maps.json` の 2 人用） |
| 実行時間 | 143.4 秒 |
| 時間切れの勝者 | 敗北 → 町の中心の数 → 忠誠度 → 人口 → 資源合計 の順で比較（`03§10` の判定要素と同じ順序）。全同値なら引き分け |

### 1.2 実測（調整後）

```
[T-M18-04] 168 試合 / 上限 15000 tick / シード 20260810,777,31337 / AI 段階 4 /
           143.4 秒 / gameOver 決着 0 件・引き分け 0 件
civ        | games | wins  | rate
yamato     |    42 |  21.0 | 50.0%
roma       |    42 |  21.0 | 50.0%
tou        |    42 |  21.0 | 50.0%
viking     |    42 |  21.0 | 50.0%
mali       |    42 |  21.0 | 50.0%
azteca     |    42 |  20.0 | 47.6%
persia     |    42 |  22.0 | 52.4%
mongol     |    42 |  21.0 | 50.0%
--- 組ごと（行 = 勝った側の勝率） ---
vs         |  yama  roma   tou  viki  mali  azte  pers  mong
yamato     |     -   50%   50%   50%   50%   50%   50%   50%
roma       |   50%     -   50%   50%   50%   50%   50%   50%
tou        |   50%   50%     -   50%   50%   50%   50%   50%
viking     |   50%   50%   50%     -   50%   50%   50%   50%
mali       |   50%   50%   50%   50%     -   50%   50%   50%
azteca     |   50%   50%   50%   50%   50%     -   33%   50%
persia     |   50%   50%   50%   50%   50%   67%     -   50%
mongol     |   50%   50%   50%   50%   50%   50%   50%     -
```

最大の逸脱は azteca の -2.4pt / persia の +2.4pt。**完了条件（±8pt）は満たしている。**

### 1.3 調整前の実測（調整の根拠）

```
yamato 47.6% / roma 47.6% / tou 47.6% / viking 47.6% / mali 47.6% /
azteca 50.0% / persia 64.3% / mongol 47.6%
```

persia だけが **64.3%**（27/42）で、他の 7 文明に対して一様に 4 勝 2 敗（67%）だった。

### 1.4 変更した数値

| ファイル | キー | 変更前 | 変更後 | 理由 |
|---|---|---|---|---|
| `src/data/civs.json` | `persia.econBonus[0]` (`startResourceAdd`) | food 200 / wood 200 / stone 100 / gold 100 | **food 50 / wood 50 / stone 25 / gold 25** | 下記 |

理由:

- `03§5` の persia の記述は「**開始資源が最多**」だけで、**量は資料に数値が無い**（`civs.json`
  の他の値と同じく実装時の初期値）。変更してよい数値である。
- 変更前の値は標準プリセット（food 200 / wood 200 / stone 100 / gold 50）に対して
  **ほぼ倍**で、開始資源だけで村人 2 体ぶんの立ち上がり差になっていた。
  10 分時点の勝敗が人口で決まる（下記 1.5）ため、この差がそのまま勝率になっていた。
- 変更後は標準に対して **+25%**。「最多」は保っており（persia 250/250/125/75）、
  他文明との立ち上がり差は村人 1 体未満に収まる。
- 掃き出し測定（persia 対全文明 42 試合、3 シード）:
  | `startResourceAdd` | persia の勝率 |
  |---|---|
  | 200/200/100/100（変更前） | 64.3%（27/42） |
  | 100/100/50/50 | 66.7%（28/42） |
  | **50/50/25/25（採用）** | **52.4%（22/42）** |
  | 0/0/0/0 | 50.0%（21/42） |
  100/100/50/50 で悪化しているのは、勝敗が人口の**しきい値**（家が建つ / 村人 1 体多く出る）で
  決まる階段状の量であり、資源量に対して単調でないため。0 は「最多」の記述に反するので採らない。

### 1.5 ⚠️ この測定の限界（数字を誤魔化さないための記録）

**現状、AI 同士の試合には文明差がほとんど出ていない。** 上の「7 文明が揃って 50.0%」は
バランスが取れているからではなく、**比べるものが盤に出ていない**ためである。根拠:

1. **AI は 30 分回しても時代が上がらない。** 8 人・2 人いずれの試合でも、45,000 tick 後の
   `player.age` が全員 **0（黎明の世）**。`03§5` の兵種ツリーは青銅の世（age 1）から
   枝分かれするので、**8 文明の固有兵・固有研究・エリートが 1 体も出ない**。
   開始時代を `teikoku`・開始資源を `high` にしても、9,000 tick 後に盤上にいるのは
   `villager` と `hunter`（黎明の共通兵）だけだった。
2. **石材と金を 1 も採らない。** `MatchStats` の累計採集量（2 人ミラー戦・45,000 tick）:
   | | 食料 | 木材 | 石材 | 金 |
   |---|---|---|---|---|
   | p0 | 741 | 420 | **0** | **0** |
   | p1 | 153 | 320 | **0** | **0** |
   金が入らないので鉄器の世（食 800 + 金 200）に永久に到達しない。
3. **採集量そのものが設計の 1/20 程度。** 30 分で食料 741。`resources.json` の
   `baseGatherRatePerSec` = 0.5 で村人 20 体なら 1,800 秒で 18,000 のはずで、
   実測は **村人 1〜2 体ぶんしか働いていない**。`setup.ts` が最初に資源へ就かせた
   3 体だけが採集し、**生産された村人は手空きのまま**であることと整合する。
4. **勝敗が全部時間切れ。** 168 試合すべて `gameOver` に到達せず（`03§10` の
   制圧・碑の写し・服属・忠誠度 0 のいずれも成立しない）、人口で決まっている。
5. **シードがほとんど効かない。** 3 シードで結果がほぼ同一（ミラー戦の最終資源が
   3 シードとも `152/223` で完全一致）。つまり有効な標本数は事実上 1 に近い。

つまり **今の 50%±8% は「文明差が実装として盤に届いていない」ことの証明**でもある。
兵種ツリーの均衡を本当に測るには、下記の申し送りを先に解く必要がある。

### 1.6 申し送り（担当外のため手を入れていない）

| # | 何が起きているか | 直すべき場所（推定） |
|---|---|---|
| A | 生産された村人が採集に就かない（手空きのまま増える） | `src/ai/econGoals.ts`。`classifyVillagers` が後から現れた村人を `VILLAGER_BUILDER` にし、建設が無いときに `gather` コマンドを出す経路が無い |
| B | 石材・金を採る判断が無い | 同上。`planEconomy` に「不足資源のノードへ村人を送る」判断が要る |
| C | `planAgeAdvance` が資源を見ずに毎回 `advanceAge` を出す（sim が黙って捨てる） | `src/ai/econGoals.ts:442`。APM を 20 倍に膨らませている原因でもある（下記 3 節） |
| D | 時代が上がらないので兵種ツリー・エリート・固有研究が未検証 | A〜C が直ってから T-M18-04 を再測定する |
| E | playerId 1 側が一方的に不利（ミラー戦で人口 13 対 6） | 原因未特定。`(tick + playerId) % interval` の位相か、開始位置の資源配置か |

A〜C が直ったあと、`tests/balance/civ.matchup.test.ts` の `MATCH_TICKS` を
30 分（45,000）へ戻して再測定すべき（そのときの実行時間は約 7 分）。

---

## 2. T-M18-03 シムの負荷

完了条件: **8 人 × 1,200 体で 1 tick 4ms 以内**
（手順書の元の条件: 45,000 tick を実時間の 1/20 以下で空回し = 2.0 ms/tick 以内）

テスト: `game/tests/perf/sim.bench.test.ts`

### 2.1 実測

```
[T-M18-03] 8 人 / エンティティ 1200 体（うち追加した兵 896 体）/ 1000 tick 計測（予熱 300）
  合計: 1546ms → 1.546 ms/tick（予算 4ms）
  参考: 45,000 tick 空回し ≈ 69.5 秒（実時間 1800 秒の 1/25.9。完了条件は 1/20 以下）
  --- 内訳（300 tick の平均、ms/tick）---
  00 rebuildGrid      0.0338
  01 applyCommands    0.0009
  02 orderDelivery    0.0002
  03 frontLifecycle   0.0519
  04 frontEnrollment  0.0093
  05 unitDecision     0.0137
  06 movement         0.2834
  07 combat           0.0356
  08 morale           0.0098
  09 economy          0.9094   ← 全体の 59%
  10 construction     0.1309
  11 production       0.0088
  12 loyalty          0.0142
  13 victory          0.0182
  14 cleanup          0.0093
  内訳の合計          1.5296 ms/tick
```

2 人戦（村人のみ・304 体）: **0.036 ms/tick** → 45,000 tick を 1.6 秒 = 実時間の 1/1111。

### 2.2 判定

**4ms 予算に対して 1.546ms。達成。** 手順書の 1/20 条件も 1/25.9 で達成。
**したがって `src/sim/**` の最適化は行っていない**（親の指示どおり、必要と判断しても
勝手に書き換えない方針。今回は必要でもなかった）。

### 2.3 それでも重い所（将来 1,200 体を超えるときに読む）

1 位が `economy` の 0.909 ms/tick で、合計の 59% を 1 システムが使っている。
1,200 体のうち採集している村人は 24 体しかない状況でこの値なので、
**体数に比例していない**（資源ノードや搬入点の全走査が疑わしい）。
2 位は `movement` 0.283ms、3 位は `construction` 0.131ms。
`combat` は 0.036ms しか使っておらず、兵を密集させても空間索引（`rebuildGrid` 0.034ms）が
効いていることが分かる。**手を入れる順番は economy → movement**。

---

## 3. T-M18-05 操作量（APM）

完了条件: **通常の操作で APM 60 未満**（`07§9` の想定は毎分 20〜40）

実装: `src/ui/stats.ts` に `TICKS_PER_MINUTE` / `apmFrom` / `MatchStats.countCommands` /
`commandsOf` / `observedTicks` / `apmOf` を追加。`snapshot()` の各プレイヤーに
`commands` と `apm` が入る（既存の欄は変えていない。新しい欄は省略可にして
既存の呼び手を壊さない）。

テスト: `game/tests/balance/apm.test.ts`

### 3.1 「通常の操作」の定義（この測定の前提）

- **1 操作 = `Command` 1 件。** `Command` は「プレイヤーが 1 回何かした」単位。
  20 体まとめての移動は 1 件（実際の操作も「囲んでクリック」の 1 手）。
  令を 6 戦域に配れば `setOrder` が 6 件。
- **操作列は `AiPlayer` の判断そのもの。** AI は `Command` しか出せない設計なので、
  出る操作の種類（令を配る・村人を出す・建物を置く・時代を進める・市場で交換する）は
  人間の操作と 1 対 1 に対応する。判断間隔は `ai.json` の数値。
- **通常 = 段階 3「諸侯」（4 秒に 1 回盤面を見直す）**。
  `06§4` の令の切り替え間隔 6 秒と釣り合う頻度。
- 段階 4（2 秒に 1 回）は「張り付いて打ち続ける人」の上限として**判定にも使う**。
- 段階 5（1 秒に 1 回）は人間ではなく機械の頻度なので**参考値**。
- 分母は **tick**（`performance.now` を使わない）。同じ操作列からは常に同じ APM が出る
  ＝ リプレイでも一致する。

### 3.2 実測（2 人戦・15,000 tick = 10 分）

| AI 段階 | 判断間隔 | 操作数 (p0/p1) | APM (p0/p1) | 判定 |
|---|---|---|---|---|
| 2 見習い | 150 tick (6 秒) | 22 / 17 | **2.2 / 1.7** | – |
| **3 諸侯（通常）** | 100 tick (4 秒) | 175 / 170 | **17.5 / 17.0** | ✅ 60 未満 |
| 4 将軍（張り付き） | 50 tick (2 秒) | 327 / 320 | **32.7 / 32.0** | ✅ 60 未満 |
| 5 覇王（参考） | 25 tick (1 秒) | 623 / 615 | **62.3 / 61.5** | ⚠️ 60 超 |

**完了条件は達成**（通常 17.5 APM、張り付きでも 32.7 APM）。
段階 4 が `07§9` の想定 20〜40 のちょうど中に入っている。

### 3.3 何が手数を食っているか

操作の種類ごとの件数（2 人合計）:

| 段階 | setOrder | produce | placeBuilding | moveUnits | releaseManual | **advanceAge** |
|---|---|---|---|---|---|---|
| 2 | 2 | 20 | 8 | 4 | 5 | 0 |
| 3 | 4 | 20 | 10 | 4 | 7 | **300** |
| 4 | 5 | 21 | 10 | 4 | 7 | **600** |
| 5 | 0 | 21 | 8 | 4 | 5 | **1200** |

**手数の 96〜97% が `advanceAge` の空打ち。** `src/ai/econGoals.ts` の `planAgeAdvance` が
資源を見ずに判断 tick ごとに毎回 `advanceAge` を出しており、資源が足りないので
sim 側で黙って捨てられている（1.6 節の申し送り C）。段階 5 が 60 を超えたのはこれだけが原因。

空打ちを除いた「本当に盤面を動かした操作」:

| 段階 | 有効な操作数（2 人合計） | 1 人あたり APM |
|---|---|---|
| 2 | 39 | 1.9 |
| 3 | 45 | **2.3** |
| 4 | 47 | 2.4 |
| 5 | 38 | 1.9 |

**人間の UI は「押せないときはボタンがグレー」（`05§4`）なので、実プレイの APM は
この 2.3 側に近い。** `07§9` の想定 20〜40 を大きく下回るが、これは
「令が半自動で回るので手数が要らない」という `01`「手数で勝たない」の設計どおりであり、
UI の見直しは不要と判断する。ただし 1.5 節のとおり **AI が内政を回せていない**ため
（村人が採集に就かない・時代が上がらない）、この 2.3 APM は
**「遊びが薄いから手数が少ない」側の値でもある**。申し送り A〜C を直したあとに再測定すべき。

---

## 4. ゴールデンリプレイ

`src/data/civs.json` を変更したので、手順書 §14.3 の手順で期待ハッシュを
`npm run golden:rebase` で再生成した。ロジック変更ではなくバランス調整のための rebase である。

変更内容: `persia.econBonus[0].startResourceAdd` を food 200/wood 200/stone 100/gold 100 →
food 50/wood 50/stone 25/gold 25（1.4 節）。

## 再測定（2026-08-10）— AI の内政を直したあと

**前回の測定（8 文明とも 50.0%）は無効。** `docs/ISSUES.md`「AI の内政が回っておらず、
文明差が盤に出ていなかった」に記録した 5 件の不具合を直したので、条件が変わった。

測定条件は前回と同じ（28 組 × 左右入れ替え × 3 シード = 168 試合、1 試合上限 15,000 tick、
AI 段階 4、約 130 秒）。

### 直した直後（調整前）

| 文明 | 勝率 |
|---|---|
| ペルシア | **83.3%** |
| 他 7 文明 | 45.2%（互いには 50.0%、ペルシアには 17%） |

内政が回るようになった結果、**開始資源の差がそのまま勝敗になった**。
前回この値を 200/200/100/100 → 50/50/25/25 に下げて「52.4%」に収まっていたのは、
内政が止まっていて差が現れなかったためで、収まっていたのではなく見えていなかった。

### 調整

`civs.json` の `persia.econBonus[0].startResourceAdd` を
**50/50/25/25 → 20/20/10/10**（食料・木材・石材・金）。

`03§5` は「開始資源が最多」とだけ定めており量は資料に無い。
**最多であることは保ったまま**、勝敗を決めない大きさにした。

### 調整後

8 文明すべて **50.0%**（逸脱 0.0pt）。

### この数字をどう読むか（重要）

**「7 文明が互いに 50.0%」は均衡ではなく、まだ差が出ていないという意味。**

- AI は 30 分で **age 1（青銅の世）まで**しか到達しない。文明ごとの兵種は青銅の世で
  枝分かれし始めるが、第 2 段（鉄器）・最終段（帝国）の兵は出ない。
- したがって現在の測定が見ているのは「開始資源」「内政ボーナス」「固有令の一部」までで、
  **固有ユニット・エリート・火器の強さは 1 度も測られていない**。
- 次に必要なのは balance 調整ではなく **AI の内政をさらに速くすること**
  （採集人数の資源ごとの配分、市場での交換の使い方、家と資源施設の建て順）。
  鉄器・帝国の世に届くようになった時点で、この表は必ず作り直しになる。

判定を通した状態で止めるのではなく、**何がまだ測れていないか**をここに残しておく。

## 再測定その 2（2026-08-10 後半）— AI の内政をさらに直したあと

### 直したこと（`docs/ISSUES.md` に詳細）

1. **農地が食料を産んでいなかった**（sim のバグ。人間のプレイヤーにも影響）。
   建設で完成した農地に食料ノードが載っていなかったので、盤上の果樹が枯れると
   食料の入り口が永久に無くなっていた。
2. **AI に探索が無かった**。石切場と金鉱は拠点から 8 マス（視界のすぐ外）にあり、
   斥候を歩かせないと一生見えない。加えて `AiView.seenResourceNodes` は
   その瞬間だけのものなので、**見つけた資源を覚える記憶**も足した。
3. 採集先の割り当てを「4 資源に順番 + 次の世に足りないものへ寄せる」の 2 本立てにした。
4. 「立ち上げ（0〜5 分）は兵を作らない」を `config.matchPhases` から引くようにした。

### 直った結果

- 4 資源すべてが増えるようになった（実測 30 分: 石材 100→798、金 50→430）
- 記憶した資源ノード 62 個（以前は視界内の森と果樹だけ）
- **青銅の世に到達**（18〜24 分）

### それでも測定は無効

| 組 | 左右そのまま | 左右入れ替え |
|---|---|---|
| ヤマト vs モンゴル | 席 1 の勝ち | 席 1 の勝ち |
| ローマ vs 唐 | 席 1 の勝ち | 席 1 の勝ち |
| ペルシア vs アステカ | 席 0 の勝ち | 席 0 の勝ち |
| マリ vs ヴァイキング | 席 1 の勝ち | 席 1 の勝ち |

**どちらの文明を置いても同じ席が勝つ。** つまり結果を決めているのは
文明ではなく**開始位置**で、文明の違いは勝敗に 1 も効いていない。
勝率が 8 文明とも 50.0% に揃うのはこのためで、均衡ではない。

原因: 兵種ツリーは**青銅の世で枝分かれ**するが、AI が青銅に上がるのは 18〜24 分。
鉄器・帝国には 30 分でも届かないので、**固有ユニット・エリート・火器・固有研究は
一度も盤に出ていない**。

### 次に直すべきこと（この順）

1. **青銅への到達を 8〜10 分に縮める**。いまの律速は食料の入り口:
   開始の果樹が枯れたあと農地に切り替わるまでに間が空く。
   `planEconBuilding` が農地を建てるのは「1 判断 1 棟」なので、
   食料が細っているときは複数面まとめて建てる判断が要る。
2. 鉄器（食料 800 + 金 200）に届かせる。金の採集人数が 1/4 では足りないので、
   次の世の費用に対する**比率で人数を割り当てる**形にする。
3. そのうえで `tests/balance/civ.matchup.test.ts` の
   「この測定が有効であることの前提」の `skip` を外す。
   **緑になって初めて上の勝率表がバランスの根拠になる。**

測定条件も変えた: 上限 15,000 tick（10 分）× 3 シード → **45,000 tick（30 分）× 1 シード**。
10 分では誰も世に上がらず、28 組すべてが「差が出る前の引き分け」になっていた。
計算量は同じで、**測れないものを 3 回測るより測れるものを 1 回測るほうがよい**。

## 再測定その 3（2026-08-10 夕）— テンポの検証と合わせて

### 変えたこと

`07§2` は「5〜12 分に最初の戦域が 1 つ立つ」と定めているが、実測（`tests/balance/tempo.test.ts`）は
**30 分のうち 20 分過ぎまで戦域が 1 本も立たなかった**。原因は AI の兵の生産条件に
「村人が目標数（18 体）に届くまで待つ」を入れていたこと ―― 18 体に届くのが 15 分ごろなので、
初接触の区間が空になっていた。**村人の数を兵の前提にしない**形に直した
（増やすのと出すのは同時に進めるもので、食料の取り合いは人間も抱えている普通の判断）。

### テンポの実測（AI 段階 4 / 2 人戦 / 30 分）

| 区間 | 資料の想定 | 直す前 | 直した後 |
|---|---|---|---|
| 立ち上げ 0〜5 分 | 戦域は立たない | 0 本 ✓ | 0 本 ✓ |
| 初接触 5〜12 分 | 最初の戦域が 1 つ | **0 本** | **1 本** ✓ |
| 分割 12〜20 分 | 2〜4 本 | 0 本 | 1 本（時間の 16%）△ |
| 総力戦 20〜30 分 | 残った戦力で決める | 1 本（5%） | 0 本 △ |

初接触は成立した。**分割（2〜4 本）と総力戦はまだ成立していない**。
どちらも「軍が十分に育たない」ことが原因で、根は内政の速さ。

### ペルシアの開始資源 ― 効き方が崖状

戦域が立つようになった結果、文明の違いが初めて勝敗に効き始めた。ペルシアの開始資源を
振ってみた実測:

| 開始資源（食/木/石/金） | ペルシアの勝率 |
|---|---|
| 20 / 20 / 10 / 10 | **92.9%** |
| 15 / 15 / 8 / 8 | **100.0%** |
| 10 / 10 / 5 / 5 | 50.0% |
| 5 / 5 / 5 / 5 | 50.0% |

**なめらかに動かず、ある値を境に 50% → ほぼ 100% に飛ぶ。**
これは「ペルシアが強すぎる」という話ではなく、**いまの AI では試合が立ち上がりの
数分でほぼ決まってしまう**ことの現れ。わずかな資源の差が村人 1 体の差になり、
それが採集量の差になって最後まで開き続ける。`07§2` が描く 30 分の起伏
（初接触 → 分割 → 総力戦）が成立していないので、勝敗が前半に張り付く。

採用値は **10 / 10 / 5 / 5**（`03§5` の「開始資源が最多」は保っている）。
`civs.json` の `note` にもこの実測を残した。

### いまの状態のまとめ

- 8 文明とも 50.0%（判定は通る）
- **ただし 7 文明は互いに区別できない**。青銅の世に誰も到達しないので
  固有ユニット・エリート・火器・固有研究が一度も盤に出ない
- ペルシアだけは差が出るが、それは兵ではなく**内政ボーナス**による
- したがって「バランスが取れている」とは言えない。**言えるのは
  「文明を無視しても勝率が偏らない程度には対称である」まで**

### 次に直すべきこと（更新）

1. **AI を青銅の世に到達させる**（いまは 30 分でも age 0 のまま。段階 4 で確認）。
   直近の律速は食料。農地の建て増しが 1 判断 1 棟なので、食料が細っているときに追いつかない。
2. 軍を育てて「分割（2〜4 本）」と「総力戦」を成立させる。
3. そのうえで `tests/balance/civ.matchup.test.ts` の
   「この測定が有効であることの前提」の `skip` を外す。
   **7 文明が互いに区別できるようになって初めて、勝率表がバランスの根拠になる。**

## 再測定その 4（2026-08-10 夜）— 青銅の世に到達させた

### 直したこと（AI の内政。5 件）

| # | 直し方 | 効果（実測・段階 4・2 人戦・30 分） |
|---|---|---|
| 1 | **次の世のぶんを取り置いてから兵を作る**（`ageReserveFx` / `canAffordWithAgeReserve`）。最初の世は全額、2 つ目以降は半分（`ai.json` の `ageReserveRatioAfterFirst`） | 取り置き前は兵 26 体・食料 0〜19 で age 0。取り置き後は**青銅に到達** |
| 2 | 取り置きの引き算を **0 で止める** | 「取り置き > 手持ち」のとき残りが負になり、**その資源を 1 も使わない兵まで作れなくなっていた**（金 50 に対し取り置き 100 で兵 0 体） |
| 3 | 採集先を「**いま必要な資源だけ**」に絞る（`gatherTargets`） | 4 資源に均等に散らしていたら、黎明の世で使い道の無い石材 800・金 599 が積み上がる一方、青銅の 500 に 25 分かかっていた。絞って**20 分に短縮** |
| 4 | 割り当ては「半分は順番・半分は次の世に足りない資源」 | 「順番だけ」にすると食料に就く人が半減して村人 15 で止まり押し切られた。「足りない資源だけ」にすると木材が細って農地が建たない（**木材 → 農地 → 食料 がひと続き**） |
| 5 | 「村人が目標数に届くまで兵を作らない」を**やめた** | 18 体に届くのが 15 分なので、`07§2` の初接触（5〜12 分）が空になっていた |

### 到達点

- **青銅の世に到達（20 分）**。村人 23 / 兵 23 / 農地 6 面 / 食料 411 で安定
- 石材と金は黎明の世では**意図的に採らない**（使い道が無い）

### 試したが採らなかったこと（実測で悪化した）

「最初の 1 隊は取り置きを無視して作る」という例外を 3 通り試して、いずれも悪化した:

| 形 | 結果 |
|---|---|
| 兵が減るたび例外が復活 | 取り置きが永久に効かず、食料 318 で止まって世が上がらない |
| 例外を一度だけ | 1 隊が死んだあと兵が出ず、**村人 22 → 2 に壊滅** |
| 例外を常に有効 | 兵は 5〜7 体で安定するが世が上がらない |

**例外なしがいちばん健全**（世が上がり、そのあと兵が 23 体まで育つ）。
コードにもこの経緯をコメントで残した。

「誰も就いていない資源へ既存の村人を移す」も試して悪化した ――
その村人が**単独で遠くの金鉱まで歩いて死に**、村人 26 → 10、食料 411 → 12 になった。
拠点から離れた資源を使うには搬入点（`mining_camp`）を先に建てる形が要る（未実装）。

### 残っている問題 ― **片方の AI が軍を作らない**

テンポの実測で分かったこと:

```
t=35000 戦域0 P0 兵8  敵拠点までの最短12 マス | P1 兵0
t=40000 戦域0 P0 兵16 敵拠点までの最短6  マス | P1 兵0
t=44999 戦域0 P0 兵23 敵拠点までの最短1  マス | P1 兵0
```

P0 は青銅に上がって兵 23 体を敵拠点の 1 マス手前まで進めるのに、
**P1（モンゴル側）が兵を 1 体も作らない**ので交戦が起きず、戦域が立たない。

つまり残っているのは「戦域が立たない」ではなく「**片方の AI が軍を作らない**」。
`docs/ISSUES.md` の「席で決まる」と同じ根で、**次に直すのはここ**。

### ペルシアの開始資源 ― 符号ごと変わった

| 開始資源 | 内政を直す前 | 直したあと |
|---|---|---|
| 20 / 20 / 10 / 10 | 92.9% | （未測定） |
| 10 / 10 / 5 / 5 | 50.0% | **0.0%（全敗）** |
| 5 / 5 / 5 / 5 | 50.0% | 50.0% |

**AI を触ると同じ値の意味が反転する。** これはペルシアの強弱ではなく、
いまの AI では試合が立ち上がりの数分で決まってしまうことの現れ。
採用値は **5 / 5 / 5 / 5**（`03§5` の「最多」は保っている）。

**データを触って勝率を追いかけるのはここで止める。**
AI のバグをデータで埋めることになり、AI が直ったらまた全部やり直しになる。

### 次に直すべきこと（更新）

1. **P1（席 1 側）が軍を作らない原因を突き止める。** これが直れば戦域が立ち、
   `tests/balance/tempo.test.ts` の skip を外せる
2. 遠くの資源を使えるようにする（搬入点を建ててから村人を送る）
3. 鉄器の世（食料 800 + 金 200）に到達させる
4. そのうえで `tests/balance/civ.matchup.test.ts` の前提テストの skip を外す

## 再測定その 5（2026-08-10 深夜）— 席 1 側が兵を作らない原因を追った

### 実測で分かったこと

P0 と P1 を並べて 30 分回し、コマンドの内訳まで比べた:

```
P0 コマンド: gather 19, produce 44, placeBuilding 18, advanceAge 1, research 3, moveUnits 7, releaseManual 13
P1 コマンド: gather 21, produce 29, placeBuilding 27       ← advanceAge も moveUnits も 0
```

P1 は**内政そのものは回っている**（村人 24 / 農地 13）のに、
`advanceAge` を一度も出さない ―― 食料が 166 で止まって 500 に届かないため。
そして世に上がらないので取り置きが解けず、兵が 1 体も出ない。

### 直した 2 件

| # | 症状 | 原因 | 直し方 |
|---|---|---|---|
| 1 | 農地を 13 面まで建てながら食料が 166 で止まる | **働き手のいない農地を建て続けていた。** 農地は食料ノードを載せるだけで、誰も就いていなければ 1 も採れない。木材を農地に吸われて家が建たず、人口 30 で詰まっていた | 農地の面数を**食料に就かせた人数まで**に制限（`pickEconBuilding`）|
| 2 | 木材が 2 まで枯れる | **平野なのに港を建てて船を作ろうとしていた。** `07§13` は平野を「水域=ほぼ無し。港と船はほぼ不要」と定めている | `maps.json` の `waterRatio` が `mapgen.aiShipMinWaterRatio`（0.05）未満なら**船を作る対象から外す**（港も建てない）|

### 効果（P1 側の実測・30 分）

| | 直す前 | 農地の制限 | + 船をやめた |
|---|---|---|---|
| 食料 | 166 | 366 | **436** |
| 農地 | 13 面 | 10 面 | 12 面 |
| 木材 | 40 | 2 | 57 |
| 兵 | 0 | 3 | 0 |

**P1 の食料が 166 → 436 まで伸び、青銅の 500 まであと少し**になった。
P0 は同じ時点で 431（500 を使って上がったあと）なので、**両者の差はほぼ消えている**
―― 残っているのは「30 分では両方が上がるには足りない」という速さの問題で、
**席による系統的な差ではなくなった**。

### 次に直すべきこと（更新）

1. **内政全体をもう一段速くする**（両者が 30 分のうちに青銅へ上がるように）。
   いまの律速は「木材 → 農地 → 食料」の連鎖の細さ。
   `villagerTarget`（段階 4 で 22）を上げるか、伐採所を増やすかを実測で決める
2. 遠くの資源を使えるようにする（搬入点を建ててから村人を送る）
3. 鉄器の世（食料 800 + 金 200）に到達させる
4. そのうえで `tempo.test.ts` と `civ.matchup.test.ts` の前提テストの skip を外す

## 再測定その 6（2026-08-10 深夜）— **戦域が立つようになった**

### 直したこと（2 件）

| # | 直し方 | 根拠 |
|---|---|---|
| 1 | 段階 4 の `villagerTarget` を **22 → 30**、段階 5 を 26 → 34 | 内政の律速は「木材 → 農地 → 食料」の連鎖の細さ。採集人数を増やすのがいちばん素直 |
| 2 | **戦域が立つ最小人数（`front.spawnMinUnits` = 3）までは取り置きを無視して兵を作る** | 戦域は「双方が 3 体以上を 15 マス内に集める」ことで立つ（`07§3`）。全額取り置くと青銅に上がるまで兵が 0 体で、30 分のあいだ戦域が 1 本も立たなかった。3 体ぶんの食料 150 は最初の世の費用 500 に対して小さいので、これだけ許しても世に上がれる |

### 取り置きの割合を振った実測（青銅到達 tick / 兵の数）

| 最初の世の取り置き | seed 20260810 | seed 7 | 戦域 |
|---|---|---|---|
| 全額（1.0）・例外なし | P0 25049 / P1 未到達 | P0 31649 / P1 22948 | **0 本** |
| 0.8（例外なし） | 両者未到達（食料が 420〜433 で張り付く）| 両者未到達 | 兵は出る |
| **全額 + 3 体までは例外** | **P0 40049 / P1 33248** | P0 未到達 / P1 34048 | **1 本（総力戦の 24%）** |

割合を 1 未満にすると、**食料が「割合 × 500」で張り付いて永久に上がらない**。
「閾値で止める」のではなく「**最小人数だけ例外にする**」形にしないと両立しない。

### 到達点

- **`tests/balance/tempo.test.ts` の「30 分のうちどこかで戦域が立つ」の skip を外して緑になった**
- 青銅の世に**両者が到達**するようになった（seed 20260810）
- 兵が 3〜16 体育ち、総力戦の区間で戦域が立つ

### まだ満たせていないこと

- **区間ごとのテンポは資料どおりではない。** `07§2` は 5〜12 分に最初の戦域、
  12〜20 分に 2〜4 本と定めているが、実測では戦域が立つのは 20 分以降で 1 本だけ
- **8 文明とも 50.0% で互いに区別できない。** 青銅に上がるのが 22〜40 分で、
  兵種ツリーの第 2 段（鉄器）に届かないため、固有ユニット・エリート・火器・
  固有研究がまだ盤に出ない。`civ.matchup.test.ts` の前提テストは skip のまま

### 次に直すべきこと（更新）

1. **青銅を 10〜12 分に早める**（いま 22〜40 分）。ここが全部の律速。
   採集の初速（開始 3 村人からの立ち上がり）と農地の建て始めの早さを詰める
2. 鉄器の世（食料 800 + 金 200）に到達させる。金を採るには遠くの鉱脈が要るので、
   搬入点（`mining_camp`）を建ててから村人を送る形が必要
3. そのうえで `civ.matchup.test.ts` の前提テストの skip を外す

## 再測定その 7（2026-08-10 深夜）— 青銅を早める試みと、そこで見えた三すくみ

### 目標は「青銅を 10〜12 分に」だったが、届かなかった

3 つのつまみを振って 4 シード × 2 プレイヤー = 8 通りで測った。
**どの組み合わせでも「青銅に上がる」と「戦域が立つ」を両方満たすのは難しい**という
構造が見えた（つまみは `ai.json`）。

| 組み合わせ | 青銅に上がった数（8 通り中） | 戦域の最大本数 |
|---|---|---|
| 村人 30 / 貯めない / 軍の下限 1 隊 | **5** | **2 本** ← 採用 |
| 村人 30 / **20 体で止めて貯める** / 軍の下限 1 隊 | 5（17〜23 分と早い） | **0 本** |
| 村人 30 / 貯めない / 軍の下限 **2 隊** | 2 | 2 本 |
| 村人 30 / 20 体で止めて貯める / 軍の下限 2 隊 | 2 | 2 本 |

読み取れること:

- **「止めて貯める」は青銅を 27 分 → 17〜23 分に早めるが、貯めているあいだ軍が細って
  戦域が 1 本も立たなくなる。**
- **軍の下限を 2 隊（6 体）に上げると戦域は確実に立つが、6 体ぶんの食料 300 が
  最初の世の費用 500 を食って青銅に届かなくなる。**
- 食料 1 種類を「村人・兵・進化」の 3 つが取り合っているので、**どれか 2 つを
  立てるともう 1 つが倒れる**。つまみの調整では抜けられない。

採用したのは **村人 30 / 貯めない / 軍の下限 1 隊**（8 通り中 5 つが青銅に到達し、
戦域も最大 2 本立つ）。`ai.json` の `villagerBankFrom` は残してあるが**使っていない**
（`econGoals.ts` にその旨をコメントで残した）。

### いまの実測（段階 4 / 2 人戦 / 30 分）

| シード | 青銅到達 P0 / P1 | 戦域の最大 |
|---|---|---|
| 20260810 | 27 分 / 22 分 | 2 本 |
| 7 | 未 / 23 分 | 2 本 |
| 99 | 24 分 / 20 分 | 2 本 |
| 31337 | 未 / 未 | 0 本 |

`tempo.test.ts` は緑（立ち上げは戦域なし・30 分のどこかで戦域が立つ）。

### 次に直すべきこと（更新）

つまみではなく**仕組み**を変える必要がある。効く順に:

1. **食料の入り口を増やす。** いま食料は「果樹（枯れる）＋農地」だけで、
   農地は木材 60 を食う。狩り（`hunter`）や漁（`fishing_boat`）を使えていない。
   食料の総量が増えれば 3 つの取り合いが緩む
2. **兵を安く済ませる。** 黎明の世の棍棒兵は食料 50。木材や石材で作れる兵が
   あれば食料の取り合いから外せる（データ側の検討事項）
3. 鉄器の世（食料 800 + 金 200）に到達させる。金には遠くの鉱脈が要るので、
   搬入点（`mining_camp`）を建ててから村人を送る形が必要
4. そのうえで `civ.matchup.test.ts` の前提テストの skip を外す

## 再測定その 8（2026-08-10 深夜）— **文明差が初めて盤に出た**

### 直したこと（2 件。どちらも sim の不具合）

| # | 症状 | 原因 | 根拠 |
|---|---|---|---|
| 1 | 食料の入り口が果樹だけで、枯れたら農地しか無い | **マップ生成が食料を果樹に固定していた。** `resources.json` は「農地・狩猟・漁・果樹・羊」の 5 種を定めているのに、`mapgen` の `NODE_OF_RESOURCE` が食料 → 果樹の 1 対 1 だった | `03§1` |
| 2 | **包囲と略奪が帝国の世まで一度も使えない** | 下段の令を「研究『二重旗』が無ければ弾く」実装だった。段は**2 枚を重ねるときの組み合わせ**を決めるもので、使用の可否ではない | `05§10`「令スロット ― 各戦域に **1 枚**。帝国の世の研究『二重旗』で 1 戦域に **2 枚まで**重ねられます」 |

2 は**資料同士の矛盾として表面化していた**: `06§13` の練習メニューは第 1 章（黎明の世）で
「反対側に略奪で囮を立て、本命の門前に包囲」を教えるのに、実装では帝国の世まで
その 2 枚が押せなかった。`05§10` の「各戦域に 1 枚」に合わせて直した。

- 二重旗なし: 段に関係なく**1 枚だけ**置ける（前の令を置き換える）
- 二重旗あり: 上段 1 + 下段 1 の**2 枚**が同時に立つ

マップには果樹 6 + 羊 4 + 狩猟 4（拠点あたり）を置くようにした。

### 結果 — 8 文明の勝率（56 試合 / 30 分 / 1 シード / 段階 4）

| 文明 | 勝率 |
|---|---|
| ヤマト / ローマ / 唐 / ヴァイキング / マリ / モンゴル | 50.0% |
| ペルシア | 57.1% |
| アステカ | 42.9% |

**判定（50%±8%）を通ったうえで、初めて文明差が現れた**
―― ペルシア対アステカが 100% / 0%（他の組はまだ席で決まる）。

そのため `tests/balance/civ.matchup.test.ts` の
**「この測定が有効であることの前提（文明を入れ替えると結果が変わる）」の skip を外して緑になった**。
これで勝率表が「文明を無視しても偏らない」だけでなく
「**文明の違いが盤に出ている**」ことの裏付けを持つようになった。

### まだ満たせていないこと

- 差が出るのは**ペルシア対アステカの 1 組だけ**。残る組は依然として席で決まる
- 鉄器・帝国の世には届かないので、固有ユニットの第 2 段・エリート・火器は未測定
- テンポの区間ごとの姿は資料どおりではない（戦域が立つのは 12 分以降で 1〜2 本）

### 次に直すべきこと

1. 鉄器の世（食料 800 + 金 200）に到達させる。金には遠くの鉱脈が要るので、
   搬入点（`mining_camp`）を建ててから村人を送る形が必要
2. 差の出る組を増やす（内政ボーナスだけでなく兵種の違いが効くところまで）
3. 統合 GPU ノートで DoD #7 を実測する

## 再測定その 9（2026-08-10 深夜）— 遠くの資源を使えるようにした

### 直したこと ― 「搬入点を建ててから村人を送る」

金鉱は拠点から離れているので、これまで AI は金を 1 も採れなかった（30 分で開始値の 50 のまま）。
村人をそのまま送ると**単独で歩いて死に**、村人 26 → 10・食料 411 → 12 に落ちる
（実測。前回記録済み）。原因は「運んだものを置く場所が無い」こと ―― 着いても拠点まで
往復するだけで運搬損失が上限に張り付く。

そこで **順序を「搬入点 → 村人」にした**:

1. 次の世が要求する資源のうち**まだ誰も就いていないもの**を選ぶ
2. その資源のいちばん近い（記憶した）ノードのそばに搬入点があるか見る
3. 無ければ**先に採掘場／伐採所を建てる**（`placeBuildingCommand` に位置を渡せるようにした）
4. 搬入点ができてから**初めて村人を 1 人送る**

搬入点を探す半径は `economy` の運搬損失の式から逆算した 40 マス
（`haulLossTilesPerStep × (haulLossMax / haulLossPer4Tiles)`）。
それより遠くに建てても損失が上限なので「そばに建てた」意味が無い。

### 対象の選び方でつまずいた点

| 選び方 | 結果 |
|---|---|
| いちばん足りない資源 | 食料の不足が常に最大になり、**金にいつまでも人が就かない** |
| `gatherTargets`（食料と木材を常に含む） | 拠点のそばで足りている木材のために遠くへ小屋を建てに行き、**青銅の到達がかえって遅れた** |
| **次の世の費用に載っている資源だけ** | 青銅（食料のみ）では何もせず、鉄器（食料 + 金）で初めて金を採りに行く ← 採用 |

### 結果（段階 4 / 2 人戦 / 30 分）

| シード | 青銅 P0/P1 | 金の残高 P0/P1 |
|---|---|---|
| 20260810 | 未 / 22 分 | 50 / **170** |
| 7 | 25 分 / 未 | **110** / 50 |
| 99 | 27 分 / 未 | **110** / 50 |

**金が初めて増えるようになった**（開始 50 → 110〜170）。鉄器の要求は 200 なので**あと少し**。
青銅に上がった側が必ず金を採り始めている（＝仕組みは動いている）。

小屋も両者 2 棟ずつ建つ。8 文明の勝率は 50.0%±7.1% で判定を維持、テンポも緑のまま。

### まだ満たせていないこと

- **鉄器の世には未到達**（金 110〜170 / 要求 200）。青銅に上がるのが 22〜27 分なので、
  残り数分では 200 まで届かない。**青銅を早めることが鉄器への唯一の道**
- 差が出る組はペルシア対アステカの 1 組のまま

### 次に直すべきこと

1. **青銅の到達を早める**（いま 22〜27 分）。ここが鉄器・固有兵・テンポのすべての律速。
   食料の入り口は増えた（果樹・羊・狩猟）ので、次は
   「開始 3 村人からの立ち上がり」と「農地を建て始める早さ」を詰める
2. 統合 GPU ノートで DoD #7 を実測する

## 再測定その 10（2026-08-10 深夜）— 青銅を 19 分に、金が要求量を超えた

### 直したこと（2 件）

| # | 直し方 | 効果 |
|---|---|---|
| 1 | **いま採っていない資源の搬入点は建てない**（`isNeededDropOff`）| 黎明の世では石材も金も使い道が無いのに、実測で 3 分の時点で採掘場（木材 100）を建てていた。木材は序盤いちばん細い資源で 100 は農地 1.7 面ぶん。やめたら**農地が 13 分で 3 面 → 6 面**に増えた |
| 2 | 段階 4 の `villagerTarget` を **30 → 22**（段階 5 は 34 → 26）| 村人 1 体は食料 50 なので、30 体まで出すと 1,350 を村人に使う。22 体に抑えると**青銅が 22〜28 分 → 19〜20 分**になり、**金が 190〜290（鉄器の要求 200 を超えた）** |

### 村人の目標数を振った実測（4 シード × 2 プレイヤー）

| 目標 | 青銅の到達 | 金の残高 |
|---|---|---|
| 30 | 22〜28 分 | 50〜170（要求 200 に届かない）|
| 26 | 21〜28 分 | 50〜180 |
| **22** | **19〜20 分** | **190〜290**（要求 200 を超える）← 採用 |

### 「AI を速くしたら本来のバランス問題が見えた」

村人 22 にしたところ、**アステカが 92.9%**（6 文明に 100% 勝つ）になった。
原因は `civs.json` の `buildSpeedMul 1.3`（村人の建設が最速。`03§7`）。
他文明の内政ボーナスは 1.15〜1.20 なので、同じ刻みに揃えて **1.2** にした
（`buildSpeedMul` を持つのはアステカだけなので 1.2 でも「最速」は保たれる）。

**これは「AI のバグをデータで埋める」のとは違う。** AI が正しく遊ぶようになって
初めて見えた本来のバランス問題なので、調整は T-M18-04 の仕事そのもの。

### その代償 ― 前提テストが再び skip に戻った

アステカを 1.2 にしたら**文明差が消えて、また席で決まる状態に戻った**
（1.3 で 92.9% / 1.2 で 50.0% という崖）。そのため
`civ.matchup.test.ts` の「文明を入れ替えると結果が変わる」は **skip に戻した**。

判断: **AI の質を優先した。** 青銅 19 分と金 200 超えは鉄器への唯一の道で、
鉄器の兵種（第 2 段）が盤に出れば内政ボーナスに頼らない本物の差が出る。
「差が出ているように見える状態」を保つために AI を遅いままにするのは順序が逆。

### いまの状態

- 青銅の到達 **19〜20 分**（4 シード中 2〜3 シードで両者到達）
- 金 **190〜290**（鉄器の要求 200 を超える）。**食料 800 が最後の壁**（実測 400〜440）
- 8 文明とも勝率 50.0%、テンポは緑
- 数値の二重管理を 1 件やめた: テストが `1.3` を直書きしていたためデータ変更で
  取り残された。**倍率は `civs.json` から引く**ように直した

### 次に直すべきこと

1. **鉄器の食料 800 を貯めさせる。** 金は足りているので、残りは食料の総量。
   農地の面数はもう働き手の数で上限になっているので、次は
   「1 人あたりの採集量」（研究「鋤」「輪作」を取らせる）が効く
2. 統合 GPU ノートで DoD #7 を実測する

## 再測定その 11（2026-08-10 深夜）— **鉄器に到達し、文明差が実体として出た**

### 直したこと（4 件）

| # | 直し方 | 効果 |
|---|---|---|
| 1 | **遊んでいる村人を採集に戻す**（`AiView.OwnEntity.state` を足して `planIdleVillagers`）| 実測で**村人 22 人のうち 14 人が遊休**、食料ノードは 10,880 も残っていた。足りないのではなく**働かせていなかった**。直したら採集が 6 人 → 16〜20 人、食料が 402 → 1,140〜3,206 |
| 2 | **進化の建物条件を満たす**（`pickAgeGateBuilding`）| `03§2` の進化条件は「前の世の建物 N 種」。費用（食料 1,414 / 金 250）を満たしていても**青銅の建物が 1 種しかなく**鉄器に上がれなかった |
| 3 | 進化用の建物 1 棟ぶんの木材を残す | 食料が細い時期に農地を建て続けて木材が 25 まで枯れ、青銅の建物（木材 150〜175）が建てられなくなっていた |
| 4 | 次の世の費用が足りているときは**手持ちがいちばん少ない資源**へ回す | 以前は食料に落としていたので、食料 3,206 余りでも人を食料に足し続けていた |

併せて sim の不具合 1 件: **`AiView` が自軍の資源ノードを建物として読んでいた**
（農地が載せる食料ノードは所有者付き）。`buildingDef` に資源ノードの typeId が渡り
「範囲外の building typeId」で落ちた。`kind` を明示して分岐するよう直した。

### 到達点

| | 以前 | いま |
|---|---|---|
| 青銅の世 | 22〜27 分（8 通り中 3〜5）| **13〜19 分（8 通り中 8）** |
| 鉄器の世 | 未到達 | **到達（25 分）** |
| 戦域 | 20 分以降に 1 本 | **初接触（5〜12 分）から立つ**。最大 3 本 |
| 食料 | 400〜440 | 415〜3,206 |
| 金 | 50 | 140〜848 |

`tempo.test.ts` は緑。`07§2` の区間の姿にかなり近づいた
（立ち上げは戦域なし → 初接触で 1 本 → 分割で 1 本 → 総力戦で 1 本）。

### 文明差が実体として出た（そしてバランス調整が未了になった）

シードを **1 本 → 2 本**に増やして標本を倍にしたところ:

| 文明 | 勝率（28 試合）|
|---|---|
| **ローマ** | **78.6%**（6 文明に 75%、アステカに 100%）|
| **ペルシア** | **67.9%** |
| ヤマト / 唐 / ヴァイキング / マリ / モンゴル | 42.9% |
| アステカ | 39.3% |

1 シードのときは刻みが 1/14 = 7.1% で判定幅（±8%）と同じだったため、
「ローマ 64.3%」が誤差か実体か区別できなかった。2 本にして**実体だと分かった**。
**データを動かす前に、まず測り方を細かくした。**

その結果:
- 「この測定が有効であることの前提（文明を入れ替えると結果が変わる）」→ **skip を外して緑**
- 「勝率が 50%±8% に収まる」→ **skip に移した**（まだ満たせていない）

**これは後退ではなく前進。** これまで 50.0% に揃っていたのは文明の違いが盤に
出ていなかったからで、いまは出ている。残っているのは**本来のバランス調整**
（T-M18-04 の本題）。

### 次に直すべきこと

1. **ローマとペルシアの強さを弱点側で釣り合わせる。**
   ローマの強み「騎兵が青銅から出る／3 系統が 3 段揃う」（`03§4`）は資料の設計なので、
   勝率を下げるなら機動力・コストなど弱点側を見る。収まったら
   `civ.matchup.test.ts` の「勝率が 50%±8% に収まる」の skip を外す
2. 帝国の世（`03§2`）への到達。鉄器に届いたので次はここ
3. 統合 GPU ノートで DoD #7 を実測する

## 追加調査（2026-08-10 深夜）— ローマの優位は戦闘ではなく内政

ローマ 78.6% の原因を切り分けた。**相性が効いていないのではないか**を最初に疑い、
同数でぶつけて実測した（`tests/unit/combat.counter.test.ts` として恒久テストにした）:

| 対戦 | 結果 |
|---|---|
| ヤマトの槍 vs ローマの軽騎兵 | **7 対 0 で槍の勝ち** |
| ヤマトの槍 vs モンゴルの軽騎兵 | **8 対 0 で槍の勝ち** |
| ヤマトの弓 vs ローマの軽騎兵 | 0 対 8 で騎兵の勝ち |
| ヤマトの槍 vs ローマの槍 | 7 対 0（守備 2 対 1 の差）|

**相性は正しく効いている**（`spear → cavalry` = good、`cavalry → ranged` = good）。
青銅の世で騎兵を持つのはローマとモンゴルだけだが、槍を持つ文明は槍で勝てる。

したがってローマの優位は**戦闘ではない**。30 分の試合はどれも時間切れ判定
（`gameOver` 0 件）なので、勝敗は**拠点数・人口・資源**で決まっている ―― つまり内政。

ローマの `econBonus` は「街道が半額」だけで、AI は街道を使わないので効いていない。
残る差は **`unitTree` が 3 系統 3 段そろっていること**の副作用と見ている:
作りたい兵の生産元（兵舎・射場・厩）を多く建てるので、
**進化の建物条件（前の世の建物 N 種）を自然に満たしやすい**。
モンゴルは騎兵しか持たない（`melee`/`ranged` が null）ので建物が増えず 42.9% に留まる。

これは資料（`03§4`「3 系統すべてが 3 段揃う唯一の文明」「穴なし＝どこも突出していない」）
の設計がそのまま出た形で、**兵の数値をいじって直す問題ではない**。
釣り合わせるなら「万能の代償」を別の形（コスト・生産速度・進化条件の扱い）で入れる必要があり、
資料に根拠のある形を選ぶ必要がある。**ここは判断が要るので次に持ち越す。**

### 次に直すべきこと（更新）

1. **ローマの「万能の代償」を資料に根拠のある形で入れる。**
   相性・兵の数値は正しいので、触るのはそこではない。候補は
   「生産元を多く持つことが進化の建物条件で有利になりすぎない扱い」など
2. 帝国の世（`03§2`）への到達
3. 統合 GPU ノートで DoD #7 を実測する
