# 02 是正プラン（進捗管理表）

このファイルが**唯一の進捗管理表**です。作業のたびに「状態」と「メモ」を書き換えてください。
凡例: ⬜未着手 / 🚧進行中 / ✅完了 / ⏸保留 / ❌やらないと決めた

優先度: **P0**=これが無いと宣伝資料と別のゲームに見える / **P1**=印象を大きく上げる / **P2**=あると良い

各タスクの具体的な手順は `03_タスク詳細.md` の同じIDを見てください。

---

## A. 方針決定（最初にここを決める。決めないとアセットが作れない）

| ID | 内容 | 優先 | 状態 | 完了条件 | メモ |
|---|---|---|---|---|---|
| A-1 | **視点の方針を決める**。推奨=トップダウン維持＋絵柄で斜め見下ろしの印象を作り、宣伝資料の `GRAPHICS` 表記を「2Dトップビューの手描き風タイル」に直す（クォータービュー化は実質フルリメイクなので却下） | P0 | ✅ | 決定が `04_スタイルガイド.md` §1 に書かれている | 2026-08-08 決定。トップダウン維持 |
| A-2 | **アートの基準（スタイルガイド）を1枚に固める**。線幅・彩度・影の形・パレット・48px/32pxの足元合わせを、宣伝資料の色を基準に文書化する | P0 | ✅ | `docs/03_.../04_スタイルガイド.md` があり、以降の生成プロンプトがそれを引用している | `04_スタイルガイド.md` を作成。彩度は S×0.78 を全タイルの基準に |

---

## B. 地面の情報量（面積が最大＝最も効く）

| ID | 内容 | 優先 | 状態 | 完了条件 | メモ |
|---|---|---|---|---|---|
| B-1 | 草地・森・砂・土・広場の各タイルに**バリエーション3〜4種**を追加し、座標のハッシュで決定論的に散らす | P0 | ✅コード / ❌絵は不採用 | 同じseedで同じ模様になる（テストでチャンクのハッシュ一致）／草原を歩いて模様の繰り返しが目に付かない | `tilemap.ts` に `tileVariant()` を実装。**本番の variant は4枚まとめて `game/` に入れること**（1枚だけ置くとそれが全面に使われる）。プレースホルダ24枚は生成済み。**❌ 本番の草地variantは実機で見て不採用にした**（生成アセットは「32pxで模様が残る粗さ」にすると必ず壁紙状になり、タイルの格子がかえって目立った。B-3の装飾デカールで情報量は足りている） |
| B-2 | **遷移タイル（autotile）**を入れる。草↔砂、砂↔水、草↔森、広場の縁。4方向＋角の16パターン | P0 | ✅コード / ⬜絵 | 海岸と広場の縁が45度の階段に見えない（`09_海岸`と比較） | 4bitマスク（上1/右2/下4/左8）。焼成時に `clear:false` で重ね焼き。プレースホルダ60枚は生成済み。⚠️ **実際に一番目立つのは `dirt`↔`grass` と `plaza`↔`dirt`** で未対応（→ B-6 を追加） |
| B-6 | **`edge_grass_dirt_*` / `edge_plaza_dirt_*` を追加**（各15枚）。広場の外周と道は `dirt` なので、B-2の指定4境界では効果が出ない | P0 | ✅コード / ⬜絵 | 広場の外周が階段に見えない | `EDGE_PAIRS` に2境界を追加（計6境界＝90枚）。プレースホルダは生成済み。二重登録を防ぐテストも追加 |
| B-3 | **装飾デカール層**（草の房・小花・小石・落ち葉・きのこ・切り株）を worldgen の決定論ノイズで配置。歩行判定には影響させない | P0 | ✅ | `08_無地の平原` と同じ座標で、無地の面積が体感で半分以下になる | **専用レイヤではなくチャンクの焼成に第3パスとして足した**（数千スプライトを毎フレーム描かずに済む／`ground` に入るので接地影より下になり順序も正しい）。草地の密度0.34。8種のプレースホルダ生成済み。同座標の比較は `after_07` vs `08_無地の平原` |
| B-4 | **海のアニメーション**。浅瀬の色帯＋波の白線を海岸線に沿って動かす。`prefers-reduced-motion` で停止 | P1 | ✅ | 海岸のキャプチャが宣伝資料の水面に見える | `render/waves.ts` を新規作成。**陸に隣接する水タイルだけ**（沖は動かさない）。1枚のGraphicsに540〜760本／スマホ120〜190本、描画バッチ3以下。**`decal` レイヤの最初の利用者**。`after_19` |
| B-5 | 木の実の木に**バリエーション（実つき／実なし／若木／枯れ木）**を用意し、在庫0で暗くするのをやめる | P1 | ✅ | 木が並んでいても同じ絵の反復に見えない | `berryTreeState()` で4状態を選ぶ（座標ハッシュで13%が若木・7%が枯れ木、残りは在庫で実の有無）。**`alpha 0.65` は削除**。⚠️ 若木と枯れ木は在庫で絵を変えない（同じ木が化けると別の木に置き換わったように見える）。**本番アセット4枚を生成・設置済み** |

---

## C. 島に「暮らしの痕跡」を置く（宣伝ヒーロー画像の中身）

| ID | 内容 | 優先 | 状態 | 完了条件 | メモ |
|---|---|---|---|---|---|
| C-1 | **家3種・風車・柵・小道**を worldgen の初期設置物として島に配置する（広場のまわり・道沿いに数軒） | P0 | ✅ | 広場から歩いて1分以内に家と風車が視界に入る／`hero.png` と並べて同じ島だと分かる | `worldgen.ts` に集落生成（+380行）。既定seedで家5・風車1・柵6・噴水1。**5つのseedで到達不能な陸0を実測**（1件ずつ置いて検査し、塞いだら取り消す＋Chebyshev2タイルのclearance）。小道は新地形を作らず既存 `dirt`（B-6の遷移がそのまま効く）。`after_08`（プレースホルダ）→ `after_09`（本番アセット）参照。**本番アセット7枚を生成・設置済み**（$0.077） |
| C-2 | **広場を円形の石畳＋噴水**にする | P0 | ✅ | `02_開始直後の広場` の四角いクリーム色が、宣伝 `screen-multiplayer.png` の広場に見える | **石畳の本番タイルを設置済み**（`after_10`）。円形マスク化。**半径4では八角形にしか見えず6に拡大**（49→137タイル）。⚠️ **spawnが広場中心から南へ3タイル移動**（噴水を中心に据えるため。噴水をずらすと `PLACE_MIN_GAP_TILES` で足元にベンチが置けなくなった）。spawnを固定値で持つコードは注意 |
| C-3 | **巣（nest）を可視化し永続化する**。夜に動物が巣で眠る絵が成立する | P1 | ✅ | 夜のキャプチャで巣に丸まった動物がいる／サーバ再起動後も巣が同じ場所にある | WeakMap→`Actor.nest` へ移し `critters_json` に保存（M3申し送り4・M5申し送り6の2回持ち越された負債を解消）。巣は `nest` 設置物として描画。**130島日で巣の数が常に「巣を持つ生きた動物の数」以下**（無限増加なし）／tick p95 0.399ms。**D-3 の睡眠ポーズが入り、夜が完成した**（`after_17`）。⚠️ **プレイ中に新しくできた巣は次の snapshot まで画面に出ない**（delta に設置物の差分が無い） |
| C-4 | **小オブジェクト**（焚き火・岩・切り株・茂み）を追加。焚き火は夜だけ点く | P1 | ✅ | 夜の広場が `screen-ecosystem.png` の夜に近づく | 茂み30・切り株12・岩12・焚き火1を決定論的に配置（`after_21`）。**岩と焚き火だけ歩行不可**にし、1件ごとに到達性を検査（5seedで到達不能な陸0）。焚き火は `lights.ts` が既に対応していて夜に自動で光る。⚠️ **attract は全部0**（餌の無い場所に群がって餓死するM3の罠を避ける）。`sim:long` で死亡0・tick p95 0ms。**本番アセット4枚を生成・設置済み** |
| C-5 | **川を作るか決める**。宣伝資料が「川むこう」「川辺」と言っている。作らないなら H-3 で資料を直す | P2 | ❌作らないと決めた | 決定が `04_スタイルガイド.md` に追記されている | 得られるのは「橋の予定地が増える」だけで、既定seedには予定地が3件そろっている。一方で到達性の保証とバランスの再検証が必要になり費用対効果が小さい。**資料側を H-3 で修正**（`04_スタイルガイド.md` §9） |

---

## D. キャラクターの見え方

| ID | 内容 | 優先 | 状態 | 完了条件 | メモ |
|---|---|---|---|---|---|
| D-1 | **接地影**（全アクター・全オブジェクト共通の楕円影）を描く | P0 | ✅ | どのキャラも地面から浮いて見えない | `render/shadows.ts`。1枚のGraphicsに全員ぶんをまとめて描く。⚠️ **スタイルガイドの `alpha 0.18` では実機でほぼ見えず 0.26 に上げた**（赤で描いて位置の正しさを確認: `after_06`）。睡眠中は縦0.6倍 |
| D-2 | **ペット5種の識別パーツを描き直す**。モフィのhalo・ミズネの水滴・ハッカの葉・モモナの垂れ耳・ホシラの星 | P0 | ✅ | `pets.png` の5匹と、ゲーム内の5匹が同じキャラだと分かる | 15枚生成（5種×3方向、`_w` は反転）$0.165。**モフィに宣伝資料どおりの水色の光輪**が入った（`after_10`）。⚠️ 併せて発見: タマゴ選択画面とペット情報パネルが `/assets/placeholder/` を**直参照**していて本番アセットに永久に切り替わらなかった → `/assets/game/` 優先に修正 |
| D-3 | **睡眠ポーズ**を追加（丸まった `_sleep` 1枚／種）＋zzzの小さな演出 | P1 | ✅ポーズ / ⬜zzz | 夜のキャプチャで動物が寝ているのが分かる（いま `alpha 0.75` のみ） | 11枚生成・設置（$0.121）。`anim==='sleep'` でテクスチャ差し替え、**無い種は従来の `alpha 0.75` に落ちる**。⚠️ 睡眠絵は**立ち絵と出所が一致するものだけ採用**（本番の立ち絵が夜だけプレースホルダに化けるのを防ぐ）。zzz は未実装 |
| D-4 | **歩行2コマ**を追加（いまは上下バウンドで代用、M1申し送り8） | P2 | ⬜ | 歩行が足踏みして見える | 枚数が倍になるので、まず動物とペットだけ |
| D-5 | **プレイヤーのアバターを4色**（フード服 紫・緑・桃・黄）にし、タマゴ選択画面で選べるようにする | P1 | ✅ | 2ブラウザで並んだとき見分けが付く（`13_雨と同一見た目` と比較） | 12枚生成＋反転4枚（$0.132）。宣伝 `screen-multiplayer.png` と同じ動物耳フード服。`Actor.species` に `'a'..'d'`、未知値は `a` にフォールバック。**旧DBは `avatar` 列を冪等ALTERし `playerId` ハッシュで振り直す**（DEFAULT 'a' のままだと古いDBだけ全員同じ色になる）。タマゴ選択画面に「じぶんのふく」の行を追加（`after_18`）。実機で桃色を選んで反映を確認。⚠️ `ActorWire.s` は delta で送られないため、色を変えても他クライアントは興味範囲を出入りするまで旧色 |
| D-6 | **いのししの絵を作り直す**（描画側の1.3倍ハックをやめる。M8申し送り5） | P2 | ✅ | 48px枠で他の動物より一回り大きく見える／`_s` が真正面 | 3枚を正方寄りで生成・設置（`_w` は反転）。**ハックは撤去済み（1.3→1.0）**。判定は画面ではなく**アセットの寸法で行った**（1.3倍は「絵が横長」への対処なので、縦横比が答え）: `critter_boar_s` は 44x48 で**高さ比1.00**＝他の動物と同じ、幅はうさぎ(27px)の1.6倍。「ひとまわり大きい」が絵そのもので出ている。`SPECIES_SCALE` の仕組みは別の種のために残した |
| D-7 | **団子状の密集を緩和する**。初期配置の分散、重なったときの描画オフセット、休憩地点の共有を減らす | P1 | ✅ | 広場周辺のキャプチャで5体以上が重なる箱が出ない | **サーバの座標は変えず描画だけ扇状にほぐした**（`crowdOffset`。golden angleで散らす／3体以上から／最大0.42タイル／影も同じずらしに追従）。シミュ側も実施: `spawn.ts` に最小間隔3.5タイル（置けなければ段階的に緩め、必ず70体置き切る）、`critter.ts` の `WEIGHTS` を `constants.ts` の `CRITTER_WEIGHTS` へ移設（M3申し送り3の負債を解消）。**30島日の前後比較は下の「D-7 の測定結果」を参照。⚠️ 効果は小さく代償がある** |


### D-7 の測定結果（30島日 / seed=long-run）

| | 変更前 | 変更後 |
|---|---|---|
| 3体以上の重なりが出た島日 | 2日（day4 / day21） | **0日** |
| 最大の重なり | 3体 | 2体 |
| 個体数 | 120で安定 | 120で安定 |
| 出生 | 53 | 52 |
| 死亡 | 3 | 3 |
| 友達の組数 | 1203 | **1110（−8%）** |

⚠️ **効果は小さく、代償がある。**

- シミュレーション上の重なりは**30島日で2日しか出ていなかった**。
  調査時に見えた「10体の団子」は、**描画側で見分けが付かなかったこと**（接地影が無く、
  ずらしも無かった）が主因で、D-1 と描画側の `crowdOffset` でほぼ解消していた
- 一方で**友達の組数が8%減った**。M3で `socializeGain` を上げてやっと成立させた
  「友達→番→出生」の余裕を削っている。出生は 53→52 なので今は影響が出ていないが、
  **冬の厳しい年に効く可能性がある**
- **もし将来 出生数が落ちたら、まずここ（spawn の最小間隔と socialize 系の重み）を戻すこと**

---

## E. UI を宣伝資料に寄せる

| ID | 内容 | 優先 | 状態 | 完了条件 | メモ |
|---|---|---|---|---|---|
| E-1 | **ペットのゲージパネル**を左上に常時表示（ハート=なつき度／水滴=おなか）。宣伝 `screen-talk.png` 準拠 | P1 | ✅ | 宣伝画像と並べて同じUIに見える | `ui/petGauge.ts`。**絶対配置にせず `.hud` の折返しflexの中に1行として入れた**（E-5でミニマップが踏んだ重なりを避ける）。390×844で重なり0を実測。おなかは E-6 で埋まった。 |
| E-2 | **右下に丸いアクションボタン3つ**（撫でる・水やり・収穫）。タッチでもキー無しで遊べる | P1 | ✅ | スマホでキー案内を読まずに撫でる・水やり・収穫ができる | `ui/actionButtons.ts`。しきい値はサーバと一致（ペット1.2 / 資源2タイル）。**押せないときは押す前に灰色になる**（従来は押してから通知が出るだけ）。収穫可否の種別判定・在庫0・1秒のクールダウンもサーバと揃えた |
| E-3 | **吹き出しを大きく・長く**。表示3.2秒→6秒程度、最大幅とフォントを上げ、ペットの真上に出す | P1 | ✅ | 会話してから目を離しても吹き出しが読める | `bubbleMs` を下限5秒・上限8秒に。`.bubble` を `font-size 0.78rem→1.12rem` / `border-radius 14→24px` / `max-width 13em→22em`。`BubbleLayer` に重なり回避（`stackBubbles`）とポップのアニメを追加。実測4.6秒後も表示されている。`after_03` 参照 |
| E-6 | **`petState` に空腹を足す**。E-1のおなかバーが「未受信」のままなので埋める | P1 | ✅ | ゲージの2段目が動く | `petState` と `PetWire` に `hunger` を追加（3箇所の送信元＋welcome）。**サーバでは反転しない**（クライアントの `fullnessRatio` が反転する。両方でやると満腹と餓死寸前が入れ替わる）ことをテストで固定 |
| E-4 | **タマゴ選択画面に本物のタマゴの絵**（5種）を出し、選ぶと孵る演出を入れる | P2 | ✅ | 「5種のタマゴから1匹」が画面で成立している | 5種を生成・設置（$0.055）。各種の特徴（モフィの光輪・ハッカの芽・ホシラの星など）が殻の模様に入っている。「この子とくらす」で0.9秒の孵化演出（揺れ→中身に差し替え）。`prefers-reduced-motion` では待たずに即決定。`after_14` |
| E-5 | **スマホのミニマップとHUDチップの重なりを直す** | P1 | ✅ | 390×844 で重なりゼロ／`layout.mobile.e2e.ts` に判定を追加 | `.hud` にミニマップぶんの `padding-right` を入れただけでは**足りなかった**。1個のチップが行幅を超えると折返さずはみ出すため、`.hud-pet` を `max-width:100%` + 三点リーダで切る対応を追加。390×844 で開/閉ともに重なり0・横スクロールなしを実測。`after_04` 参照 |

---

## F. 時間帯・天気・季節の演出

| ID | 内容 | 優先 | 状態 | 完了条件 | メモ |
|---|---|---|---|---|---|
| F-1 | **夜を濃くする**（`alpha 0.3`→0.5前後の紺）＋星空と月のオーバーレイ | P0 | ✅ | 夜のキャプチャが一目で夜だと分かる（`07_夜` と比較） | `effects.ts` に `NightSky`（星120個・月の満ち欠け8島日周期）。night 0.3→**0.5** / evening 0.16→**0.26**。同一セッションで計測: 草地の輝度 **昼221→夜132（−40%）** / 森 **158→62（−61%）**（以前は−20%）。`after_01` `after_02` 参照 |
| F-2 | **光源（加算光）**。ランタン・焚き火・家の窓を夜だけ光らせる | P0 | ✅ | 夜の広場に光の玉があり、その周りだけ明るい | `render/lights.ts` を新規作成。lantern/campfire/observatory/house_* に対応（未実装の種類は光らせないだけで落ちない）。⚠️ 加算なので**明るい地面（広場）や光源の重なりで白飛びする**。実機で広場が白い塊になったため `intensity` を 0.85→0.5 等へ下げた |
| F-3 | **雨を強くする**。地面の濡れ・水たまりの波紋・雨音（`audio.ts` の合成音）／霧・曇りも調整 | P1 | ✅ | 「雨の日」だと画面と音で分かる | 粒 260→420、線の濃さ 0.35→0.5、**濡れた地面の被せ**と**水たまりの波紋**（22個・1.1秒周期）。雨音は**音源ファイルなし**でホワイトノイズ＋ローパス/ハイパスで合成。⚠️ 雨天の実機確認は未実施（時間帯・天気の再現手順は `AI_CODING.md` §11） |
| F-4 | **季節の色調**。春=花、夏=濃い緑、秋=紅葉、冬=雪と枯れ木 | P1 | ✅色調 / ⬜木の絵 | 4季節のキャプチャが並べて別物に見える | `SeasonTint` を新設（時間帯tintとは**別レイヤ**。1枚にすると夜と秋の色を混ぜる必要が出て互いに壊れる）。春=桃0.10/夏=緑0.12/秋=橙0.16/冬=白0.20。⚠️ **4季節の実機確認は未実施**。紅葉・雪の木のアセットは未着手 |

---

## G. 宣伝で謳っていて未実装のもの

| ID | 内容 | 優先 | 状態 | 完了条件 | メモ |
|---|---|---|---|---|---|
| G-1 | **建設中の見た目（足場）と貢献UI**。予定地に足場を描き、近づくと「みんなで作る」パネルが出る | P0 | ✅ | 島を歩いていて橋・井戸・天文台の予定地に気づける（`11_天文台の予定地に何も無い` と比較） | `render/constructions.ts` で足場＋進捗バー（半分で桃→緑）。`after_05` 参照。貢献パネルは `ui/buildPanel.ts`（名前・進捗・自分の貢献回数・残り回数・**押せない理由**）。実機で 0%→15% と自分の貢献3回を確認（`after_20`）。⚠️ クールダウンは**プレイヤー単位で工事をまたいで共有**されるので、その旨を文面に出している |
| G-2 | **天文台の完成アセットが無い**（`obj_observatory.png` が存在しない） | P1 | ✅ | 完成させると天文台が描かれる | 天文台は**ランタンで代用されていた**ので `observatory` 種別を新設。さらに**完成した井戸も画面に出ていなかった**（`water` 資源は地形で表現するため描画対象外）ので `well` の設置物も置くようにした。本番アセットも生成・設置済み（ドーム＋望遠鏡。リポジトリ直下の `.env` での疎通確認を兼ねて生成） |
| G-3 | **「おでかけ／記念撮影」の扱いを決める**。区画は作らず「その場の画面を1枚保存・共有」に読み替えて実装するか、宣伝資料から落とす | P1 | ✅ | 実装するかH-1で資料を直すかが決まり、`04_スタイルガイド.md` に追記されている | **区画は作らないと決定**（プレイヤーごとの土地は設計に無い概念）。「記念撮影」だけを `ui/snapshot.ts` で実装。`renderer.extract.canvas` で画面を撮り、枠と「ぽこもふ島／N日目・季節と時間帯・ペット名」を焼き込む。⚠️ `frame` を渡さないと stage 全体（4096px超）が対象になって詰まる／`clearColor` を渡さないと背景が透過する。実機で `pokomofu-1日目-1848.png` の保存を確認（`after_22`）。資料側は H-1 で修正 |
| G-4 | **荒廃度を見えるようにする**。`tilemap.ts:125` のTODOを実装し、荒れタイル率を体感できる水準に上げる | P1 | ✅ | 収穫を繰り返した畑が茶色く荒れて、時間で戻るのが見える | M3申し送り2（最大0.7%では見えない）。描画は `decayTint()` で実装済み（0→白 / 100→`#a8926e`）。**⚠️ 荒廃度をクライアントへ送るプロトコルが存在しない**（→ G-6）／バランス調整も残り |
| G-6 | **荒廃度の送信プロトコルを追加**。`protocol.ts` に `chunkDecay` を足し、`hub.ts`/`sync.ts` から興味範囲のチャンクぶんを低頻度（1島時間に1回程度）で送る | P1 | ✅ | クライアントで `setChunkDecay()` が呼ばれ、荒れが画面に出る | 1島時間ごと＋**変化があったチャンクだけ**。実測1通556バイト・帯域+4B/秒/人（現状5.4KB/秒に対し+0.1%）。`decayPerHarvest` を 8→20 に上げ、畑を採り切ると真っ茶色→約2島日で戻る。⚠️ **水面が茶色く濁る不具合を併せて修正**（水資源は飲まれるたび荒廃度が上がり p50=99 になる。シミュは正しいので描画だけ除外した。`after_13`） |
| G-5 | **設置物の撤去**。サーバに `remove()` はあるがクライアントのメッセージが未定義 | P2 | ✅ | 置いたベンチをその場で撤去できる | `remove` メッセージを追加。**自分のものだけ**（島所有の家・風車・柵・噴水・井戸・天文台・巣は不可）／1タイル以内／1秒1回。実機で「ベンチを置いた→ベンチを片づけた」を確認 |

---

## H. 宣伝資料側の是正（実装が固まってから）

| ID | 内容 | 優先 | 状態 | 完了条件 | メモ |
|---|---|---|---|---|---|
| H-1 | A-1 と G-3 の決定を `index.html` に反映（`GRAPHICS` の表記、「記念撮影」の行） | P1 | ✅ | 資料に、実装に無い機能が1つも書かれていない | `GRAPHICS` を「2D クォータービュー」→**「2D トップビュー」**に。「おでかけ：友だちの区画へ遊びに行き、記念撮影」→**「記念撮影：気に入った風景を1枚の絵として残せる」**（区画は作らないと決定） |
| H-2 | 画面イメージを**実際のスクリーンショットに差し替える**（B〜F が終わってから） | P1 | ✅ | `※画面イメージ（生成AI）` の注記を外せる状態になっている | 4枚すべて実写に差し替え（1200×800で撮影 → 既存と同じ1536×1024）。`hero`=集落と全景 / `screen-ecosystem`=夜に丸まって眠る動物＋焚き火と月 / `screen-talk`=LLMが生成した吹き出し / `screen-multiplayer`=広場に3人（紫・黄・緑）とペット。**`.bubble-text` のCSS重ね書きを撤去**（実写に吹き出しが写っているため不要）、figcaption を「実際の画面」に、フッターの「開発前のイメージであり、生成AIで作成」を「実際に動作しているゲームのスクリーンショットです」に修正。`after_25` `after_26` |
| H-3 | 「川むこう」「川辺」の記述を、C-5 の決定に合わせて直す（川を作らないなら「岸辺」等へ） | P2 | ✅ | 島に無いものを指す表現が残っていない | 3箇所を修正（「川むこうの木の実」→「岬のむこうの木の実」／「川の向こうが気になる」→「島の北がわが気になる」／「川辺で自然に出会う」→「海辺で自然に出会う」）。`index.html` に「川」の語は**0件**になった |

---

## 完了の判定（このプラン全体のゴール）

次の3つが揃ったら完了とします。

1. `01_調査結果.md` の各キャプチャと**同じ座標・同じ時間帯・同じ天気**で撮り直したものを並べ、
   すべて「宣伝資料と同じゲーム」に見える
2. **H-2 が終わっている**（宣伝資料の画面イメージが実スクリーンショットに置き換わっている）
3. `npm run typecheck` / `npm test` / `npm run test:e2e` がすべて通り、
   初回ロードが5MB以内・10秒以内、スマホ30fps以上を維持している（M8の完了条件を壊していない）
