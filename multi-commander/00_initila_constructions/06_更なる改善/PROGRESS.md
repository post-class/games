# 06 更なる改善 — 実装進捗

このファイルは**中断・再開のための唯一の記録**です。作業した人・エージェントは必ず更新してください。

- 計画: [specs/02_改善案の詳細.md](specs/02_改善案の詳細.md) / [specs/03_進め方とロードマップ.md](specs/03_進め方とロードマップ.md)
- 開始: 2026-08-10
- ベースライン（着手前）: `npx tsc --noEmit` エラー0 / `npx vitest run` **531 passed / 47 files**

## 確定した方針（ユーザー判断済み・変更禁止）

| 論点 | 決定 |
|---|---|
| 撃墜されたときの戦役進行 | **案A＋再挑戦を促す**。進行は止めない（勝利点減・記録に「機体喪失」）が、デブリーフで「この任務をやり直す」を既定の選択肢にして目立たせる |
| 第1章の必須目標 | **「輸送船を帰投航路に乗せる」＋「自機帰投」**。5分の制限時間も必須にする。救助・敵阻止は加点 |
| コクピットの視界 | **中央60%を抜く**。黒帯で狭めず、風防の柱と天蓋の縁を描いて四隅を締める |

## 第1期 — 遊びとして成立させる

| ID | 内容 | 状態 | 担当 | 所有ファイル |
|---|---|---|---|---|
| T1-① | 勝敗を成立させる（必須目標・達成度3段階・加点表示） | 完了 | agent-win | `src/mission/**`, `src/content/veil/missions/**`, `src/content/missions.ts`, `src/app/App.ts` |
| T1-② | 撃墜と戦死を事件として見せる（無線・警告・撃墜の間） | 完了 | agent-drama | `src/app/game.ts`, `src/hud/**`, `src/audio/**`, `src/content/dialogue.ts`, `src/content/pilotDialogue.ts` |
| T1-⑥ | 敵機を見えるようにする（縁光・曳光・距離帯） | 完了 | agent-visibility | `src/render/**`（`Cockpit.ts` を除く） |
| T1-⑦ | コクピットを既定表示にする（柱・天蓋・中央60%） | 完了 | agent-cockpit | `src/render/Cockpit.ts`, `src/styles/cockpit.css`, `src/app/settings.ts` |

状態の値: `未着手` / `作業中` / `実装済み(未検証)` / `実装済み(検証中)` / `完了`

### 保留（第1期では入れない）
- T1-⑥ から要望のあった設定キー4件（`enemyRimLight` / `farContactLights` / `backgroundIntensity` / `tracerBoost`）は
  **第1期では追加しない**。まず既定値の見え方を固めるのが目的で、選択肢を増やすと評価がぶれるため。第2期の ⑧ と合わせて判断する。

### 第1期の完了条件（specs/03 より）

- [x] 難易度「ふつう」で第1章を通し、**帰投しただけでは勝てない** — 輸送船を放置して帰投 → 「任務失敗」/勝利点 -2/目標一覧が 失敗・達成 を出し分け
- [~] 難易度「やさしい」で第2章を通し、**敵機の姿を見ながら1機以上撃墜できる** — **未達（要人手確認）**。敵の曳光弾が明確に見えること、敵機が画面内に入りターゲット枠と ITTS が付くことは確認できたが、headless ブラウザは離散キー入力しか送れず（押しっぱなしができない）ドッグファイトが成立せず、撃墜まで到達できなかった。**実機のマウス操縦での確認が必要**
- [x] 僚機が撃墜されたとき、**その場で気づく** — 僚機の被弾段階で顔付きの無線（「Sable: 装甲に被弾。支援をお願いします。」）を確認。中央告知は DOM で文言を確認
- [x] 自機が撃墜されたとき、**撃墜されたと分かってから**画面が切り替わる — 撃墜中に `.mc-casualty`「撃墜された／機体喪失 — 記録に残る」と `.mc-damagestage`「ハル危険域 — Alt+E で脱出できる」が表示中であることを DOM で確認
- [x] コクピット視点と外部視点のキャプチャが**一目で別物と分かる** — コクピットは風防の柱・天蓋・計器盤、外部視点は計器盤が消えて最小HUD「外部視点 [F] / SPD / THR / TGT / NAV / 目標」に置換

## 第2期 — 結果が積み上がる

| ID | 内容 | 状態 | 担当 | 所有ファイル |
|---|---|---|---|---|
| T2-③④ | 4状態を出撃結果から動かす／章末選択を出撃結果から生やす | 完了 | agent-narrative | `src/app/narrative.ts`, `src/app/App.ts`, `src/ui/ChoiceScene.ts`, `src/content/veil/chapters.ts` |
| T2-⑤ | Nav到着の間合い・タイマー開始・旋回時の速度損失・出撃時スロットル・ch03のescortArrive化 | 完了 | agent-pacing | `src/sim/nav.ts`, `src/sim/flight.ts`, `src/mission/**`, `src/content/veil/missions/**`, `src/content/missions.ts` |
| T2-⑧⑨⑭ | 目標表示3行と可読性・警告2行・Navラベル切れ・被弾方向・チュートリアル | 完了 | agent-readout | `src/hud/**`, `src/styles/cockpit.css`, `src/app/game.ts`, `src/ui/Tutorial.ts` |


### 第2期の完了条件（specs/03 より）

- [x] **デブリーフの数値と章末選択の中身が出撃結果で違う**
      「見捨てた」側を実走: 救助0のため選択肢から「救難」が消え **「今からでも、港に救難隊の派遣を要請する」** に差し替わり、
      状況説明も「格納庫に降ろすものは何もない。…」へ変化。**帰還者は 0（最低）のまま**（以前は 0→80 に跳ねていた）。
      デブリーフに内訳が1行ずつ出た:
      `帰還者 +1　+2（救助 1名 / 僚機 1名が生還）　−1（自機喪失）` / `軍令信用 -12　−15（任務失敗 / 未達成の条件 2件 / 機体喪失）` /
      `方針「…」 航路信頼 +5 / 軍令信用 -3`（**選択の効果が出撃結果より小さいことが読める**）。
      「守って帰る」側の実走比較は未実施（ドッグファイトが headless では成立しないため）。単体テストで両側を固定済み。
- [x] **Nav 到着時にその Nav の目標が画面に写っている**
      第2章 NAV 1 到着直後の実測で、周囲の対象が **0.7k / 1.7k / 2.1k / 2.3k**。キャプチャ `.tmp/p06w/06-arrival.png` に船が大きく写る
      （以前は 11.4km / 25km で何も映らなかった）
- [x] **制限時間つき目標が到着してからカウントを始める**
      NAV 1 到着時点ではタイマー行が出ず、NAV 2（救難区域）到着後に
      `＋ 残り 2:32 — …救難区域到着から2分50秒で先遣隊が航路情報を持ち帰る` が出て減り始めた
- [x] **目標表示が背景に埋もれない**
      目標行の背景に `rgba(4, 12, 14, 0.72)` の帯が敷かれていることを実測。常時3行以内に収まり、
      4本目以降は `他2件` と件数で示される。**太陽と文字がちょうど重なる構図の実写は取得できていない**（帯は常時適用）
- [x] **撃たれた方向が画面から分かる**
      被弾の瞬間を捉えて実測: `.mc-hitedge left` opacity 0.38 / `bottom` 0.463、`.mc-hitdir` = **「被弾 下方向」**。
      キャプチャ `.tmp/p06w/11-hitdir.png` に画面端の赤い光と文字が写っている

### 実機確認で分かったこと（記録）
- チュートリアルが `]` 1回で **1/6 → 2/6** に進むようになった（以前は進まなかった）
- 難易度ふつうの出撃時スロットルが **35% / 140 KPS**（以前は 0%）
- 第2章の漂流者カウンタが **1/3** と総数を正しく出す（以前は 0/0）
- 操縦ヒントが画面中央に出ず、自動で消える
- 撃墜時に「撃墜された／機体喪失 — 記録に残る」＋管制の無線
  「船体が保たない。Alt+E で脱出できる。判断は任せろ。」→「機体が消えた。救助艇を出す。」が出る
- デブリーフで撃墜されたとき「この任務をやり直す」が既定フォーカスになっている

### 人手で確認したい項目（headless では取れないもの）
headless ブラウザはキーを押しっぱなしにできず（離散入力のみ）、機体を狙った位置へ運べない。
以下は**実機のマウス操縦での確認が必要**。

1. **敵機の姿を見ながら1機以上撃墜できる**（第1期からの引き継ぎ）
2. **収容の完遂**（T4-⑮）。実機で確認できたのは「収容準備 — 接近せよ — 残り 807m（300m 以内へ）」まで。
   300m 圏へ入って3秒保ち「〈名前〉を収容」まで到達する流れ、途中で加速したときにバーが巻き戻ること、
   「速すぎる — 減速せよ」の表示は**未確認**（単体テスト38件では固定済み）
3. **決闘の成立**（T4-⑯）。周りの帝国機が撃ってこなくなることの目視
4. 旋回時の速度保持 44%→62% が「追いつける」感触になっているか
5. 側壁を暗くした結果、暗い星域でコクピットの枠が黒く潰れすぎていないか
   （潰れすぎなら `Cockpit.ts` の `FRAME` 色と内装 PointLight 0.22 が調整点）

### ④ の判断（確定）
「何もできなかった章では選択画面を出さない」案は**採用しない**。章の進行とテストを壊すリスクがあるため、
**選択肢の中身を出撃結果で変える**方向にする。10章すべてで必ず2つ以上の選択肢が出ることをテストで固定する。

## 第3期 — 帰る場所を作る

| ID | 内容 | 状態 | 担当 | 所有ファイル |
|---|---|---|---|---|
| T3-⑩⑫ | 艦内パネルの切れを全部直す／名鑑に顔画像と絞り込み／戦況マップを星系図に | 完了 | agent-hub | `src/ui/HubPanels.ts`, `src/styles/ui.css`, `src/ui/ScreenHost.ts` |
| T3-⑪⑬ | 会話を2〜3往復＋返事2択／関係値の段階／機体の性格の一言／主人公選任の「変わること」 | 完了 | agent-people | `src/app/roster.ts`, `src/app/barTalk.ts`(新規), `src/content/pilotDialogue.ts`, `src/content/ships.ts`, `src/ui/PilotSelectScene.ts`, `src/app/App.ts` |
| T3-補 | ch04/06/07 を「飛んで着けば勝ち」でなくする／加点表記を全章へ | 完了 | agent-chapters | `src/mission/**`, `src/content/veil/missions/**`, `src/content/missions.ts` |

### 担当間の受け渡し（決めた型）
`src/app/barTalk.ts`（agent-people が新規作成）が次をエクスポートし、agent-hub が描画する。

```ts
export interface BarTalkView {
  pilotId: string;
  turns: Array<{ speaker: 'pilot' | 'player'; text: string }>;
  replies: Array<{ id: string; label: string }>;  // 空配列なら会話終了
  relation: { label: string; step: number; max: number };
}
```

- `HubContext.barTalk?: BarTalkView` の追加は agent-hub 側。会話の状態は `App.ts`（agent-people）が持つ。
- `ShipDef.character?: string`（機体の性格の一言）は agent-people がデータを入れ、agent-hub が格納庫で描く。

### 第3期の完了条件（specs/03 より）

- [x] 艦内のどの画面でも、**文章が枠で切れていない**
      切れの主犯は `.mc-screen:not(.briefing) > .mc-panel` の `flex: 0 1 auto` + `max-height: min(42vh, 390px)` **1指定**で、
      6画面すべてがこれで切られていた。1280×720 では `@media (max-height: 760px)` の `max-height: 34vh` が効いて本文が約244pxに縮み、
      名鑑の36名が3名しか読めなかった。すべて `flex: 1 1 auto; max-height: none` にして下の空白を使い切る形に
- [x] 名鑑から**36名すべて**を読める。1/5ページのページ送り（`,` `.`）＋絞り込み（`J` 勢力 / `K` 生死 / `L` 章）。**顔画像つき**
- [x] 酒場で誰かと話すと**2往復以上**あり、こちらの返事を選べる
      実機で `相手→自分→相手→自分→相手` の5ターンを確認。関係が **初対面 → 顔見知り** に変化。
      ゲージ `■□□□` と理由行 `直前の出撃では一緒に飛んでいない` も出る
- [x] 戦況マップに**地図が描かれている**（960×500 の SVG。8戦域・章の順路・現在地リング・4状態を「効く場所」に配置）
      ※ 当初は十章一覧が先に出て地図が画面外へ押し出されていたので、veil では**地図を先頭**に並べ替えた
- [~] 格納庫の4機を並べたキャプチャで、**形の違いが分かる** — **未確認**。
      `ShipDef.character`（性格の一言、文と実数値の一致を機械照合済み）は入ったが、
      シルエット画像そのものの描き分けは未着手。第5期以降の課題

## 第4期 — 本家超えの軸を立てる

第3期と並行して着手できるものから始めている（ファイル所有が重ならないもの）。

| ID | 内容 | 状態 | 担当 | 所有ファイル |
|---|---|---|---|---|
| T4-⑯ | 敵エースへの通信（名乗る/降伏勧告/決闘）・ポッドを撃たない選択が次に効く・再会で会話が積み上がる | 完了 | agent-ace | `src/content/aces.ts`, `src/content/dialogue.ts`, `src/ui/CommsMenu.ts`, `src/sim/ai.ts`, `src/app/game.ts`, `src/app/save.ts`, `src/audio/**` |
| T4-⑮ | 救助を自分の手で行う操作にする（減速・静止・収容／搭乗者名／連れ帰った名前の一覧） | 完了 | agent-rescue | `src/sim/**`, `src/mission/**`, `src/hud/**` |
| T4-⑰ | 「発砲ゼロ」「誤射ゼロ」を主役にした章 | 未着手 | — | `src/content/veil/missions/**`（着手可能） |

### ⑮ が本家超えの本命
本作は「撃墜数ではなく誰を帰したか」を指標に掲げているのに、**救助がプレイヤーの操作になっていない**。
救助が操作になった瞬間に、このゲームは本家と比較する必要がなくなる。T3-補 の完了を受けて着手済み。

`src/app/game.ts` は T4-⑯ 担当が編集中なので、収容の入力配線は担当が**報告し、私が適用する**手順にしている。
（自動で成立する設計にできるならそのほうが望ましいと指示済み。）

## 引き継ぎ状況

### 第1期・第2期で解決した引き継ぎ（記録）
- 敵機の視認性（曳光弾・縁光・距離帯） → T1-⑥ で対応。**撃墜までの人手確認だけ未了**
- 出撃時スロットル 0% → T2-⑤ で全難易度に初速（やさしい50%/他35%）
- 画面端の Nav ラベルの切れ → T2-⑧ で画面内へ押し戻し
- ch03 が「飛んで着けば勝ち」 → T2-⑤ で `escortArrive`(min=12) 化。**ch04/06/07 は未対応**（監査結果をテストに記録済み）
- 加点表記が表示のみ → T2-③ で4状態を出撃実績から動かすようにして接続
- 第1章の「隔壁5分」で輸送船が実際に沈む挙動 → **未実装のまま**（時間超過は確実に任務失敗になる）

## 第3期以降

**第1期・第2期は完了。** 第3期に着手できる状態。

- 第3期: ⑩艦内パネルの切れ / ⑪会話の往復 / ⑫戦況マップの地図化 / ⑬機種と主人公の差
- 第4期: ⑮救助を操作にする / ⑯敵エースとの関係 / ⑰発砲ゼロの章

### 第3期に持ち込んだ課題（すべて解消）
- ch04 / ch06 / ch07 の勝敗成立 → 解消（ch04 は `escortArrive`、ch06/ch07 は `recon`）
- 加点目標の `reward` を全章へ → 解消（`(任意)` のフォールバックは消滅）

### 残っている課題（第5期以降）
- **格納庫の4機のシルエットが同じ星形**。`character`（性格の一言）は入ったが図の描き分けは未着手
- **ch02 の漂流者3名に名前がない**。「八十三年前の船から戻ってきた」設定で現役名簿に該当者がいないため。
  名簿へ失踪者の項目を足すか、名前を決める必要がある（`ch02.ts` に TODO）
- 第1章の「隔壁5分」で輸送船が実際に沈む挙動（勝敗は成立済み。TODO 残置）
- 太陽と目標文字がちょうど重なる構図の実写確認（帯は常時適用なので優先度は低い）
- 酒場・名鑑のパイロット表記のうち、`pilots.ts` 由来の callsign 併記（`桐谷 綾（キリタニ アヤ）`）は
  酒場側に残っている（名鑑は統一済み）

## 作業ログ

新しいものが上。

| 日時 | 内容 |
|---|---|
| 2026-08-10 | T4-⑮ 実装（**933 passed / 74 files**、`tsc` エラー0、`vite build` 成功）。救助を操作にした: `src/sim/recovery.ts`（純粋な保持判定。表面間 `radius` 以内／相対速度 60m/s 以下／3秒保持、外れると1.5倍速で戻る）＋ `src/hud/recoveryHud.ts`（「収容中 2.4s / 3.0s」「速すぎる — 減速せよ」「接近せよ — 残り 640m」）。**キーを増やさず自動保持**にしたので `game.ts` の配線は1行（`recoverySuspended`）だけ。`rescue` 目標は**種別を増やさず置き換え**（全 `rescue` が操作必須）。`SpawnGroupDef.displayNames` で1基ごとの搭乗者名を宣言（ch01=confed-13/14/15、ch04=confed-16/17/18、ch08=kilrashi-09/06）。`summary().rescuedNames` を追加。`MissionDef.rollCall` で第10章の最終無線が**自分が連れ帰った名前だけ**を一人ずつ読む。T4-⑯ からの依頼で `ace-pod:` タグを誤射・民間損害から除外 |
| 2026-08-10 | **第2期の実装完了**。`tsc` エラー0 / `vitest` **745 passed 62 files** / `vite build` 成功 / `git diff --check` クリーン |
| 2026-08-10 | T2-③④: 4状態を出撃実績から動かすようにした（`sortieNarrative`）。**「帰還者 0→80」の原因は章データの `returnees: 7` が 人数×10 で +70 になっていたこと**。重みは 出撃 ±12 / 選択 ±5（帰還者は ±6名 / ±2名）。デブリーフに増減の内訳を理由つきで表示。章末選択に条件を付け、10章×10種の出撃結果で選択肢が常に2つ以上出ることをテストで固定 |
| 2026-08-10 | T2-⑤: 到着時の間合いを「出現位置の群単位の引き寄せ」で担保（群の中心 ≤2.6km / 端 ≤4.8km。第1章の先遣隊 11.4km→3.2km、輸送船 25km→最至近1.7km圏）。第3章の避難船は既定式で**片側11.9km**に伸びていたので隊列幅を片側1.4kmに制限。`timeLimit.startAtNav` でタイマーを到着後開始に。旋回時の速度保持を 44%→62%（`TURN_ALIGN_BOOST=1.4`。技量差テストが崩れない範囲で最大）。出撃時スロットルを全難易度で初速あり（やさしい50%/他35%）。ch03 を `escortArrive`(min=12) 化 |
| 2026-08-10 | T2-⑧⑨⑭: 目標の常時表示を3行＋暗い帯に、全目標一覧は既存の `V` で右VDU3ページ目へ（新キーなし）。警告を2行に。被弾方向を画面端の光＋装甲図のハイライトで表示。**チュートリアルが進まなかった原因は、第1期で完了条件が「進行判定には使用しない」と無効化され B キーでしか進まなくなっていたこと**。艦内ハブに帯が残ったのは `App.onMissionEnd()` が `game.endMission()` を呼んでいなかったため |
| 2026-08-10 | 統合修正（私）: `game.ts` が目標の必須/加点と残り秒を**表示文から正規表現で逆算**していた脆い経路を撤去し、`MissionRunner.objectiveViews()` の `required` / `timeLeftSec` を素通しに。`App.ts` の護衛生存数の数え直しを撤去し `summary().escortSurvivors/escortTotal` を直読みに。旧挙動を固定していたテスト1件を「逆算しないこと」を固定する形へ書き換え |
| 2026-08-10 | MCP 復帰後に**第2期のブラウザ確認を実施（1280×720 / 難易度ふつう / 新規戦役）**。完了条件5項目を確認。証跡は `.tmp/p06w/` |
| 2026-08-10 | T3-⑪⑬ 完了（818 passed / 67 files）。**主人公選任は着手前は選んでも何も変わらなかった**（`save.protagonistId` の参照が2箇所だけ）。最小の実効果としてブリーフィングに `搭乗: 〈名前〉 “二つ名”` を出し、画面に「変わること／変わらないこと」を明記。酒場は2往復固定＋返事2択、関係値を5段階（不信/初対面/顔見知り/信頼/盟友）に。**「話す＝一律 bond +0.08」で連打すると無限に関係値を上げられた**ので撤去し1出撃1回に制限。機体4機に `character` を追加し文と数値の一致をテストで機械照合 |
| 2026-08-10 | T3-補 完了（818 passed）。ch04 に `escortArrive`、**ch06 と ch07 は `recon`（撃たずに記録する）を必須**に。加点表記 `reward` を**全10章の任意目標**に付与し、`(任意)` のフォールバックは消滅。第1章の「輸送船が実際に沈む」挙動は**入れなかった**（勝敗は既に成立しており、沈めると失敗理由が三重に出て見え方を検証できないため。TODO は残置） |
| 2026-08-10 | **指示との相違を1件受理**: ch07 は `escortArrive` を指示したが担当が `recon` を採用。理由は ch07 の `TAG.escort` が「停止命令を執行する連邦哨戒機」＝護送対象ではなく進路を塞ぐ側で、必須にすると急進派が哨戒機を落とした瞬間に**操作で回避できない詰み**が生まれるため。妥当と判断し採用 |
| 2026-08-10 | **第3期・第4期(⑯⑮)の実装完了**。`tsc` エラー0 / `vitest` **938 passed 75 files** / `vite build` 成功 |
| 2026-08-10 | T4-⑮ 完了: **救助が操作になった**（本家超えの本命）。`rescue` を「半径に入れば自動」から**保持型**に置き換え（距離=目標半径 / 相対速度 ≤60 m/s / 3秒保持 / 外れると1.5倍速で戻る / 案内は条件距離の4倍から）。キーは追加せず「低速で近づいたら自動的に始まる」設計にしたので `game.ts` の配線は1行だけ。収容した搭乗者名を `rescuedNames` で積み、第10章の読み上げに繋げた |
| 2026-08-10 | T4-⑯ 完了: エース通信（名乗る/降伏勧告/決闘）を通信メニュー6番に**入れ替え**で追加（新規キー0件）。決闘成立でエース陣営の他機が自機を狙わなくなる。座席を撃つ/撃たないが次章の態度を変える |
| 2026-08-10 | 統合修正（私）: エースの脱出ポッドを**誤射・民間損害から除外**する判断（識別ミスでも民間損害でもなく「敵エースの誓約」が正しい宛先）。`narrative.ts` に座席の扱い4項目を追加して誓約へ配線（撃った1件 -8 / 助けた1件 +5）。`save.ts` に `rescuedNames` を追加（旧セーブ耐性つき）。デブリーフに「連れ帰った者」行を追加。`ACE_POD_TAG_PREFIX` の二重定義を解消。**名鑑の表記を `speakerName()` 経由に統一**（`朝倉 澪（アサクラ ミオ）` と `Amina Okafor（アミナ・オカフォー）` の混在を解消） |
| 2026-08-10 | 自分のコメントの誤りを1件訂正: 誓約で「座席を1件撃てば必ず負になる」と書いたが、合計クランプ前に素の和を取るため救難を積めば正で終わる。**特例で負に固定はしない**（内訳の各行と適用値が食い違うため）。実挙動どおりの記述に直しテストで固定 |
| 2026-08-10 | 実機確認(4) 1280×720: 主人公選任の表記統一と「変わる/変わらない」明記／戦況マップの星系図（並べ替え後）／酒場の2往復＋関係が初対面→顔見知り／名鑑36名がページ送りで顔つき／第2章で `収容準備 — 脱出ポッド 接近せよ — 残り 807m（300m 以内へ）` と目標文 `300m 以内で減速し3秒保つ`、カウンタ 0/3 を確認 |
| 2026-08-10 | T2-⑤ 実装。(1) Nav 到着時の間合い: `src/mission/navArrival.ts` を新設し、Nav に紐づく主目標の群を「到着した自機から 2600m 以内」へ引き寄せ＋隊列幅を片側 1400m に制限（最悪 9.1km → 中心 2.6km / 端 4.8km）。(2) `timeLimit` に `startAtNav` を追加し、第1章の3本を救難区域到着から計時（隔壁5分は required 維持）。(3) 旋回の速度損失を緩和（横方向の推力予算 ×1.4）。ホーネット全速横旋回 175 → 250 kps。(4) 出撃時スロットルを全難易度で 0% にしない（やさしい 50% / 他 35%）。(5) ch03 に `escortArrive`（避難船12隻を NAV 3 へ）を required 追加。(6) T2-③④ からの依頼で `summary()` に `escortSurvivors` / `escortTotal`（`ESCORT_KINDS` 再利用）を追加。(7) T2-⑧ からの依頼で `objectiveViews()` に `required` / `timeLeftSec` を追加（表示文の正規表現からの逆算を不要にした。`startAtNav` 未開始は undefined）。テスト `t2b-nav-arrival` 27件追加。`tsc` エラー0 / `vitest` 744 passed 62 files / `vite build` 成功。ブラウザ確認は未実施 |
| 2026-08-10 | T1-① 追補: ブリーフィングの目標表記を HUD と同じ出所（`objectiveRewardPrefix`）に統一。護衛対象の固有名を `SpawnGroupDef.displayName` として宣言し、`MissionRunner.escortTargets()` / `displayNameOf()` を公開（game.ts 側の推測をやめられる）|
| 2026-08-10 | T1-① 実装。`escortArrive` 目標種別を新設し第1章の必須目標を4つ（護衛帰投・護衛生存・隔壁5分・自機帰投）に組み替え。任意目標を加点表示（reward）に、デブリーフを達成度3段階＋機体喪失記録に変更。テスト `tests/ut/t1a-objective-win.test.ts` 14件追加（545 passed / 48 files）。ブラウザ確認は未実施 |
| 2026-08-10 | T1-② 再開分完了: 開口部クリップの配線（枠・ラベル・ITTS・マーカーを開口部内だけに描き、外は縁の矢印へ）と、護衛対象名を正式API（`displayNameOf` / `escortTargets` / `isEscortTarget`）へ差し替え。推測コードは撤去 |
| 2026-08-10 | **第1期 完了**。`tsc` エラー0 / `vitest` **643 passed 56 files** / `vite build` 成功 / `git diff --check` クリーン |
| 2026-08-10 | 実機確認(3) 1280×720: 緑の横線が消滅、側壁が周囲と馴染み黒潰れもなし、照準環が中央、敵が開口部外のときターゲット枠と ITTS が消え開口部の縁に矢印だけが出ることを DOM 実測で確認 |
| 2026-08-10 | README.md を実装に追従（`escortArrive`、達成度3段階と加点表記、コクピット既定ON と開口部クリップ、被弾5段階と撃墜の間、テスト件数 643/56） |
| 2026-08-10 | T1-⑦ 追加分完了: 緑の横線の原因は 3D 内装の `EDGE`（MeshBasicMaterial=光源も露出も受けない原色）と判明し、マテリアルごと削除。柱と側壁に帯・溝・縦リブ・リベットを追加し明度を落とした。開口部の寸法は不変（画面幅65%・開放面積34.5%）。631 passed |
| 2026-08-10 | T1-① 追加分完了: `SpawnGroupDef.displayName` を名前の唯一の出所にし、`escortTargets()` / `displayNameOf()` / `isEscortTarget()` を公開。全10章の護衛対象に固有名を宣言し網羅テストで固定。636 passed |
| 2026-08-10 | T1-② の追加分が API タイムアウトで中断。`project.ts` の補助関数と `HudView` の `viewRect/isReadable/clippedArrow` までは実装済みで、**描画側への配線が未完**（tsc が未使用変数2件で落ちる）。同じ担当に続き＋護衛API差し替えを再依頼 |
| 2026-08-10 | 実機確認(2) 1280×720 / 1366×768 / 1920×1080。**既存の不具合を1件修正**: 計器盤の詰め組みが `@media (max-height: 760px)` の中にしかなく、**768px と 1080px では左VDUの PRIMARY 行・右VDUの WINGMAN 行が下端で切れていた**（計器盤の高さは最大 170px なのに 10.5px 組みは 178px 必要）。詰め組みを全画面高で既定にして解消。3解像度で `clipped: false` を実測 |
| 2026-08-10 | T1-② 再開分完了: 開口部クリップの配線（枠・ラベル・ITTS・マーカーを開口部内だけに描き、外は縁の矢印へ）と、護衛対象名を正式API（`displayNameOf` / `escortTargets` / `isEscortTarget`）へ差し替え。推測コードは撤去 |
| 2026-08-10 | **第1期 完了**。`tsc` エラー0 / `vitest` **643 passed 56 files** / `vite build` 成功 / `git diff --check` クリーン |
| 2026-08-10 | 実機確認(3) 1280×720: 緑の横線が消滅、側壁が周囲と馴染み黒潰れもなし、照準環が中央、敵が開口部外のときターゲット枠と ITTS が消え開口部の縁に矢印だけが出ることを DOM 実測で確認 |
| 2026-08-10 | README.md を実装に追従（`escortArrive`、達成度3段階と加点表記、コクピット既定ON と開口部クリップ、被弾5段階と撃墜の間、テスト件数 643/56） |
| 2026-08-10 | T1-⑦ 追加分完了: 緑の横線の原因は 3D 内装の `EDGE`（MeshBasicMaterial=光源も露出も受けない原色）と判明し、マテリアルごと削除。柱と側壁に帯・溝・縦リブ・リベットを追加し明度を落とした。開口部の寸法は不変（画面幅65%・開放面積34.5%）。631 passed |
| 2026-08-10 | T1-① 追加分完了: `SpawnGroupDef.displayName` を名前の唯一の出所にし、`escortTargets()` / `displayNameOf()` / `isEscortTarget()` を公開。全10章の護衛対象に固有名を宣言し網羅テストで固定。636 passed |
| 2026-08-10 | T1-② の追加分が API タイムアウトで中断。`project.ts` の補助関数と `HudView` の `viewRect/isReadable/clippedArrow` までは実装済みで、**描画側への配線が未完**（tsc が未使用変数2件で落ちる）。同じ担当に続き＋護衛API差し替えを再依頼 |
| 2026-08-10 | 実機確認(2) で見つけた残課題3件を差し戻し: (a) 開口部の下端に明るい緑の横線が1本走る (b) 風防の側壁が無地の明るい灰色スラブで未完成に見える (c) 敵機がコクピット構造に隠れている位置でもターゲット枠とラベルが構造の上に描かれる |
| 2026-08-10 | 敵機の視認性は「曳光弾が長い赤い筋として明確に見える」「敵機が画面内に入るとターゲット枠と ITTS が付く」ところまで確認。**撃墜までの確認は headless の離散キー入力では不可能**（押しっぱなしができない）。実機マウス操縦での確認が必要 |
| 2026-08-10 | 4体すべて実装完了。統合修正を実施 (`game.ts` から `cockpit.update(hullRatio)` を呼ぶ / SettingsPanel の項目名を「コクピット表示 (風防・計器盤)」へ / **照準環 `.mc-reticle` を top 35% → 50%**) |
| 2026-08-10 | `tsc` エラー0 / `vitest` 613 passed 54 files / `vite build` 成功 |
| 2026-08-10 | 実機確認(1): 旧セーブ移行OK (cockpitDecorations false→true、他の値は保持)。コクピット枠が既定で表示。目標に加点表記。**第1章を「輸送船を守らず帰投」で飛んで正しく「任務失敗」**(以前は「任務達成」)。目標一覧が 失敗/達成 を出し分け |
| 2026-08-10 | 実機確認で見つけた要修正2件を各担当へ差し戻し: ①ブリーフィングの目標一覧に加点表記が未反映 ②護衛対象の喪失が戦闘中にまったく通知されない・失敗表示を読む前に画面が切り替わる |
| 2026-08-10 | 第1期の4タスク（T1-①②⑥⑦）をサブエージェント4体に並列で着手。ファイル所有を分割して衝突回避 |
| 2026-08-10 | 改善案資料を `specs/` に作成。ユーザー判断3点を確定。ベースライン記録（531 tests passed） |
| 2026-08-10 | T1-② 実装。被弾段階を `src/hud/damageStage.ts` に一本化（シールド喪失→装甲被弾→ハル被弾→ハル危険域30%）。ハル危険域で赤い縁取り＋警報音＋`Alt+E` 案内、僚機の被弾3段階と戦死の無線・中央告知、自機撃墜に4.2秒の間（カメラを爆発へ、入力停止）、外部視点でダッシュボードを最小HUDに置換。テスト `t1b-damage-stage` 13件 / `t1b-hud-drama` 11件 / `t1b-wingman-radio` 6件 追加。ブラウザ確認は未実施 |
| 2026-08-10 | T1-② 追補。護衛対象（protect/escortArrive/protectCount/holdTag のタグ）の被弾を3段階の無線＋ハル危険域アナウンスで通知し、喪失時は中央に「〈艦名〉 喪失」＋管制の無線。任務失敗の見出しを読む時間を `LOSS_READ_DELAY=3.6秒` として確保（達成時の4.2秒は不変）。テスト `t1b-escort-drama` 13件追加（629 passed / 55 files）|
| 2026-08-10 | T1-② 追補2。コクピット構造の裏にいる相手へ枠を描かない対応: `openingRectPx` / `pointInRect` / `rectEdgeArrow` を `src/hud/project.ts` に追加し、ターゲット枠・ラベル・ITTS リード・機体/Nav マーカーを `COCKPIT_OPENING`（render 側の値を読むだけ）の内側に限定、外は開口部の縁の矢印へ。装飾 OFF と外部視点は従来どおり全画面。護衛対象の名前推測（`escortTagsOf`/`escortNamesOf`）を撤去し `MissionRunner.isEscortTarget()` / `escortTargets()` / `displayNameOf()` へ差し替え。テスト `t1b-cockpit-clip` 7件追加・`t1b-escort-drama` を正式APIへ更新（643 passed / 56 files）|
